# LFMN 创新思路与实验记录

更新日期：2026-09-16
状态：仅使用当前阶段特征的反馈支路已完成结构与优化诊断，未取得可辨认增益；停止该具体路线，不进入长训练。

## 1. 核心问题与研究假设

现有 LFMN 的 8 个重建阶段都从共享浅层特征 `fs` 生成 SFML 调制参数 `beta`、`gamma`，并计算 `fm = beta * prev + gamma`。这让各阶段持续获得浅层结构先验，但生成调制参数的支路看不到该阶段的当前特征 `prev`，因此无法直接根据“哪些纹理尚未恢复”调整调制。

**创新思路 1：阶段反馈的纹理感知调制（暂名，非论文定名）。** 在原 SFML 旁增加一条小型反馈支路，使调制修正同时参考共享浅层特征、当前阶段特征，以及从输入 LR 图像提取一次的梯度/边缘提示。研究假设是：这种反馈更有利于重复纹理、密集线条等结构复杂区域的重建，可能提升 Urban100、Manga109 的 PSNR，同时尽量保持轻量。**这是待验证假设，不是已取得的效果；结构复杂区域变好也不保证数据集平均 PSNR 变高。**

## 2. 拟议实现

当前代码入口为 `LFMN/model/lfmn.py` 中的 `SFML` 类与 `Net.forward()`。保留原权重可加载的主路径；新增轻量反馈支路将 `fs`、压缩后的 `prev` 和 LR 梯度图融合，预测对原 `beta`、`gamma` 的小幅残差修正。新增支路输出以零初始化，使新模型在训练开始时尽可能复现原模型输出，再逐步学习修正。优先只在第 3、5、7 阶段启用；具体阶段数通过消融决定，不能先认定该组合最优。

目标是验证“使用当前阶段信息的反馈”是否比同等参数量的普通加宽更有效。作为首轮约束，×4 模型以当前约 760K 参数为参照，新增参数尽量控制在约 3% 内；推理延迟与 FLOPs 同时测量，不以参数量代替真实速度。该约束是实验设计目标，不是已测结果。

高频损失、知识蒸馏和 TAB 动态计算分配可以作为后续独立实验，首轮不与反馈支路同时引入，避免无法归因。特别是高频损失可能改善视觉边缘却降低 PSNR，须单独消融。

## 3. 当前可用基线

以下为已有预训练权重的**评测记录**，并非新模型结果。`repro/results_summary.txt` 标注开启 `--self_ensemble`；该文件有重复项和缺项，正式实验前应重新整理完整基线日志。

| 模型/权重 | 数据集 | 放大倍率 | PSNR (dB) | SSIM | 自集成 | 来源 |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 原 LFMN / `scale4_model_939.pt` | Urban100 | ×4 | 26.904 | 0.8087 | ×8 | `repro/results_summary.txt` |
| 原 LFMN / `scale4_model_939.pt` | Set14 | ×4 | 28.951 | 0.7892 | ×8 | `repro/results_summary.txt` |
| 原 LFMN / `scale4_model_939.pt` | B100 | ×4 | 27.794 | 0.7437 | ×8 | `repro/results_summary.txt` |
| 原 LFMN / `scale4_model_939.pt` | Manga109 | ×4 | 31.405 | 0.9191 | 需复核原命令 | `experiment/all_runs/test/log.txt` |
| 原 LFMN / `scale4_model_939.pt` | Set5 | ×4 | 32.507 | 0.8990 | 否（单次推理） | `experiment/all_runs/baseline/x4_single/log.txt`，2026-09-15 重跑 |

模型参数量由本地实例化计算：×2 为 676,491，×3 为 780,411，×4 为 759,627。论文报告的 ×4 FLOPs 为 57.2G；本地尚未重新测量 FLOPs 和延迟。**Urban100 ×4 超过 27.00 dB 仅是首阶段研究目标，绝非预期收益保证。**

## 4. 正式实验顺序与判据

1. 整理基线：检查数据集 LR/HR 配对、权重加载、逐图结果以及自集成开关。当前评测为输出量化后、Y 通道、裁去 `scale` 像素计算 PSNR；所有对照保持一致。
2. 先做独立的 LRSA 回拼对照。`patch_reverse()` 对部分尺寸的恒等输入无法原样回拼：按真实 `step = patch_size - 2` 测试，57×73、patch 16 时约 1.37% 像素由 1 变为 0.75。比较原回拼与按覆盖次数归一化后的逐图 PSNR；预训练权重可能适应了原行为，所以也要观察微调结果。此项不算作反馈支路收益。
3. 取得与论文/实验一致的 DIV2K 训练数据，固定训练步数、学习率、裁块、数据增强和随机种子。先做“原 LFMN 同条件微调”，再做“原 LFMN + 反馈支路”。工作区目前只有 benchmark 数据，尚不具备完整训练条件；`LFMN/option.py` 的默认训练配置也不同于所附论文。
4. 分别消融：只加入当前阶段特征、只加入梯度提示、二者同时加入；再比较启用阶段数，并加入相近参数量的普通加宽对照。至少用 3 个随机种子报告均值和波动；同时看 Urban100、Manga109、Set5、Set14、B100，不只挑选有利数据集。
5. 分开报告单次推理与 ×8 自集成的 PSNR/SSIM。跨服务器可比较相同协议的 PSNR，但比较延迟应使用同一硬件、软件环境、输入尺寸及计时方法。记录参数、FLOPs、峰值显存和实际延迟。

只有当相同数据与评测协议下，新模型相对同条件训练的原模型有稳定增益，且参数与延迟仍符合轻量目标，才能得出“创新提高了 PSNR”的结论。

### 4.1 逐项创新与训练起点

创新可以逐项提出、逐项验证，但论文中的最终模型应围绕一个清晰的核心问题组织，不能仅因为三个模块叠加后分数更高就称每个模块都有效。建议先完成 A（阶段反馈）相对基线的实验；若再提出 B（例如回拼以外的真正新模块）和 C，则分别训练并测试 `基线`、`A`、`B`、`C`、`A+B`、`A+C`、`B+C`、`A+B+C` 中与研究论点有关的组合。组合的贡献以相同协议下的增量判断；若 B 单独有效而 A+B 无增益，应如实报告交互或冗余。

**不要求每次从随机初始化训练。** 在方案筛选阶段，可让所有实验组从同一份原 LFMN 预训练权重出发，分别微调相同训练步数；新支路零初始化，原模型也做同等步数的微调作为对照。这样成本低，能比较“加模块”带来的净收益。组合模型 `A+B` 也应从共同的起点独立训练；如果先训 A 再接 B，必须给其他组匹配总训练步数，并明确这是分阶段训练实验，否则额外训练量会混入创新收益。

准备正式论文结论时，至少对最有希望的结构做从零训练（随机初始化）并与从零训练的原 LFMN 对照，尽量遵循原论文训练数据和训练预算；如果资源不允许，也可以明确限定结论为“基于公开预训练权重的微调改进”，不可写成从零训练的通用架构优势。优先用多个随机种子重复最终对照，报告平均值与波动。评测集只用于评测，不用于挑选训练轮数或反复调参。

## 5. 实验结果登记表

你后续给出结果时，在此表新增一行；不要覆盖既有记录。无法确认的字段标记“待确认”，不同评测口径分开成表。

| 日期 | 实验编号 | 改动/对照 | 数据集 | 倍率 | PSNR | SSIM | 自集成 | 参数 | FLOPs | 延迟/设备 | 权重或日志路径 | 备注 |
| --- | --- | --- | --- | ---: | ---: | ---: | --- | ---: | ---: | --- | --- | --- |
| 待填 | E00 | 原 LFMN 同条件训练基线 | Urban100 | ×4 | 待测 | 待测 | 待确认 | 待测 | 待测 | 待测 | 待填 | 创新对照所必需 |
| 待填 | E01 | 回拼归一化独立对照 | Urban100 | ×4 | 待测 | 待测 | 待确认 | 待测 | 待测 | 待测 | 待填 | 与创新模块分开 |
| 待填 | E02 | 原 LFMN + 阶段反馈支路 | Urban100 | ×4 | 待测 | 待测 | 待确认 | 待测 | 待测 | 待测 | 待填 | 主要研究假设 |

每次结果最好同时提供：运行命令、模型/权重名称、训练数据与步数、随机种子、数据集和倍率、是否自集成、PSNR/SSIM、设备与耗时、日志路径。若只有 PSNR，也先如实登记，其他字段留待确认。

## 6. 参考依据

- 项目附带论文：`LFMN.pdf`；原实现：`LFMN/model/lfmn.py`。
- CATANet，CVPR 2025：https://openaccess.thecvf.com/content/CVPR2025/html/Liu_CATANet_Efficient_Content-Aware_Token_Aggregation_for_Lightweight_Image_Super-Resolution_CVPR_2025_paper.html
- SAFMN，ICCV 2023：https://openaccess.thecvf.com/content/ICCV2023/html/Sun_Spatially-Adaptive_Feature_Modulation_for_Efficient_Image_Super-Resolution_ICCV_2023_paper.html
- AFSMNet，IEEE Signal Processing Letters 2025（此处依据出版方摘要）：https://ieeexplore.ieee.org/document/10909534/
- NTIRE 2025 Efficient SR Challenge Report：https://openaccess.thecvf.com/content/CVPR2025W/NTIRE/html/Ren_The_Tenth_NTIRE_2025_Efficient_Super-Resolution_Challenge_Report_CVPRW_2025_paper.html

## 7. 创新性边界（投稿前必须核对）

“反馈”并非新概念：[SRFBN（CVPR 2019）](https://openaccess.thecvf.com/content_CVPR_2019/html/Li_Feedback_Network_for_Image_Super-Resolution_CVPR_2019_paper.html)已用反馈细化超分特征。本文档所述支路目前只是可实验的假设，不能直接称为独创方法。若其效果成立，还须与 SRFBN、SAFMN、AFSMNet 等工作明确区分机制（例如：LFMN 中共享浅层先验与当前阶段状态联合调制、纹理难度条件控制及额外成本），做相应对照和消融；若没有足够区别，应继续寻找更有说服力的核心问题，而不是仅改名称或叠加模块。
## 8. 训练基础设施修正记录

- 2026-09-15：`LFMN/data/div2k.py` 接入官方 `DIV2K_valid_HR` 与 `DIV2K_valid_LR_bicubic/X4`，并正确映射 0801–0900 的编号。100 对验证图像读取测试通过。
- 2026-09-15：修复当前 PyTorch 版本中 `MultiStepLR.last_epoch` 初始为 0 时，`--epochs 1` 在首轮训练前被 `Trainer.terminate()` 判定结束的问题。以 2 个真实 DIV2K batch 和验证图 0801 完成训练、验证及模型保存集成测试。
- 上述属于数据/训练基础设施修正，不能计作算法创新或 PSNR 增益。该集成测试的 26.438 dB 只涉及随机初始化后 2 个 batch 和 1 张验证图，不进入正式实验对比表。
- 2026-09-16：修复 Windows 多进程数据加载时重复写入 `config.txt`：将 checkpoint 创建移入 `main()`，避免 worker 导入主模块时产生文件写入副作用。以 4 个 worker、1 个训练 batch 和验证图 0801 回归，配置只写入 1 次且 `model_1.pt` 正常保存。此项同样属于训练基础设施修正。
- 2026-09-16：RTX 4060 Laptop GPU 上完成 ×4 从零训练短跑。配置为随机种子 1、DIV2K 训练图 0001–0800、HR patch 256（LR patch 64）、batch 4、L1、Adam、初始学习率 2e-4、20 个 batch；在验证图 0801–0810 上得到 26.795 dB / 0.7578。第 10、20 batch 的累计平均 L1 分别为 9.1021、7.9827；验证前向 74.99 秒，保存了 `model/model_1.pt` 和优化器状态。日志位于 `experiment/all_runs/sanity/x4_scratch_20b_20260916_092158/`（本地输出，不提交 Git）。该结果只证明训练、验证和保存链路正常，训练量和验证范围均不足，不能作为正式基线或创新收益。
- 2026-09-16：RTX 4060 Laptop GPU 上完成 ×4 从零训练的完整第 1 epoch：1000 个 batch（4000 个裁块）、随机种子 1、DIV2K 0001–0800、HR patch 256（LR patch 64）、batch 4、L1、Adam、学习率 2e-4。epoch 末累计平均 L1 为 7.5470；验证图 0801–0810 得到 28.285 dB / 0.8092；墙钟 11 分 11 秒，其中验证前向 84.69 秒。用户提供的 5 秒采样监控中最高温度 67°C、最高显存占用 7924 MiB。输出位于 `experiment/all_runs/baseline/x4_scratch_epoch1_20260916/`。该结果用于吞吐、散热和稳定性评估；只训练 1/1000 epoch 且只验证 10 张图，不作为正式基线。
- 2026-09-16：补齐长时间训练的断点恢复。优化器保存时同步保存 `scheduler.pt`；旧检查点缺少该文件时按已完成 epoch 重建闭式学习率，避免在 200/400/600/800 epoch 里程碑重复衰减。复制 epoch-1 检查点后成功续训到 epoch 2，并通过 epoch 200 恢复为 1e-4 的边界测试。此项属于训练基础设施修正。
- 2026-09-16：租用 AutoDL 单卡 RTX 4090 24GB 实例（16 核 CPU、120GB 内存、50GB 数据盘）。固定服务器基础环境为 Ubuntu 22.04、Python 3.12.3、PyTorch 2.5.1+cu124、CUDA runtime 12.4；`torch.cuda.is_available()` 为 True，空闲温度 30°C。该环境将先运行与笔记本一致的 batch 4、10 张验证图协议，实测吞吐后再决定长期训练。
- 2026-09-16：服务器首次20-batch启动在构建数据加载器时因环境清单遗漏 `scikit-image` 而退出，尚未进入模型训练。已将 `scikit-image==0.25.2` 加入 `requirements-server.txt` 和环境自检；该失败不登记为实验结果。
- 2026-09-16：AutoDL RTX 4090 24GB 完成 ×4 从零训练20-batch短测（服务器环境 PyTorch 2.5.1+cu124、Python 3.12.3；其余训练参数与笔记本短测一致）。验证图 0801–0810 得到 26.805 dB / 0.7581，验证前向 9.56 秒，完整墙钟 19 秒；输出为 `experiment/all_runs/sanity/server_4090_x4_20b_v2/`。笔记本同协议为 26.795 dB / 0.7578、验证前向 74.99 秒；短测 PSNR 相差 0.010 dB，说明迁移后的数据与评测链路基本对齐。该结果仍只用于环境迁移验证，不进入正式模型对比表。
- 2026-09-16：AutoDL RTX 4090 24GB 完成 ×4 从零训练完整第 1 epoch：1000 batch、累计平均 L1 7.6396；验证图 0801–0810 为 28.369 dB / 0.8097；验证前向 9.64 秒，墙钟 3 分 14 秒。5 秒监控采样最高温度 59°C、最高显存 10783 MiB。笔记本同协议墙钟 11 分 11 秒，服务器约快 3.46 倍；服务器与笔记本 epoch-1 PSNR 相差 0.084 dB，正式基线和创新模型必须在同一服务器环境中重新训练。按当前速度线性估算1000 epoch约53.9小时、按2.18元/小时约117.5元；预留10%约59.3小时、129.2元。输出为 `experiment/all_runs/baseline/server_4090_x4_epoch1/`；该结果仍是吞吐与迁移试验，不是最终论文基线。

## 9. 阶段反馈首版实现与高效筛选方案（2026-09-16）

已新增独立模型 LFMNFeedback，原 LFMN 基线文件保持不变。首版只使用当前阶段特征，不同时加入 LR 梯度提示，以便单独判断“阶段状态反馈”是否有效。第 3、5、7 阶段分别使用一个轻量支路：48 通道经 1×1 压缩至 8 通道、深度 3×3 卷积，再预测 beta 与 gamma 的有界残差修正。输出投影为零初始化，因此加载同一 LFMN 权重时初始输出与基线完全一致。

本地结构验证结果：LFMN ×4 为 759,627 参数；反馈版本为 763,635 参数；新增 4,008 参数，占 0.528%。使用公开 scale4_model_939.pt 和固定随机输入验证，初始最大输出差为 0.0；反馈输出层梯度非零，说明新支路可学习。这些是实现正确性结果，不是 PSNR 改进结果。

首轮采用“成对短程微调”筛选。基线组与反馈组均从相同公开预训练权重开始，使用相同 DIV2K、随机种子、训练轮数、学习率和验证范围。服务器脚本默认执行 10 epoch、学习率 1e-5、验证图 0801–0810，预计两个实验合计约 1 小时左右，实际以日志为准。先观察逐 epoch 曲线和最终差值；若反馈组没有稳定优势，则调整阶段位置或结构，不直接投入 1000 epoch。

论文主表允许引用其他论文按其公开协议报告的 PSNR/SSIM，不要求逐一复现所有对比方法。每个数字必须记录论文来源、数据集、倍率、是否自集成及评测口径；若协议不一致，应分表或明确标注。本文自己的基线、创新模型和消融必须在同一服务器环境与同一协议下训练和评测。最接近本方法的少量方法可优先使用官方权重进行本地复核，以增强审稿可信度，但这不等于复现所有方法。

服务器并不会使同一算法天然获得更高 PSNR；它主要缩短训练时间并提高长时间运行稳定性。论文结论只使用同一 RTX 4090 环境中的配对对照，笔记本与服务器之间的 PSNR 差异不作为创新收益。
- 2026-09-16：首轮成对筛选中的原 LFMN 微调组已完成 10 epoch；随后创新组在模型动态导入阶段因文件名与加载器的小写命名规则不一致而退出，错误为找不到 model.lfmnfeedback。该失败发生在模型创建和训练之前，没有产生创新模型结果，也不需要重跑已完成的基线组。已将创新模型入口改为加载器实际查找的文件名，并把动态导入纳入自动检查。此项属于实现修复，不计作算法实验。

## 10. E02 阶段反馈首版成对筛选结果（2026-09-16）

实验环境为 AutoDL RTX 4090 24GB、Python 3.12.3、PyTorch 2.5.1+cu124。两组均从同一 scale4_model_939.pt 权重开始，使用 DIV2K 1–800、×4、HR patch 256、batch 4、L1、Adam、学习率 1e-5、随机种子 1，分别微调 10 epoch；验证范围为 0801–0810，未使用自集成。基线输出目录为 experiment/all_runs/screen/feedback_20260916_112938/baseline_ft_10e；反馈模型输出目录为 experiment/all_runs/screen/feedback_20260916_112938/feedback_s357_m8_ft_10e_v2。

| 组别 | 参数量 | 最终 PSNR | 最佳 PSNR | 最终 SSIM | 最佳 SSIM | 10轮 PSNR 均值 | 墙钟 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 原 LFMN 同条件微调 | 759,627 | 29.784 | 29.791（epoch 8） | 0.8367 | 0.8368（epoch 9） | 29.7848 | 32分02秒 |
| LFMNFeedback，阶段3/5/7、mid=8 | 763,635 | 29.788 | 29.788（epoch 5） | 0.8367 | 0.8368（epoch 2） | 29.7858 | 33分18秒 |

反馈组减基线组的逐 epoch PSNR 差值为：+0.008、-0.005、+0.002、+0.001、+0.002、-0.005、+0.009、-0.003、-0.003、+0.004 dB。最终值高 0.004 dB，10轮均值高 0.001 dB，但最佳值低 0.003 dB；最终 SSIM 相同，最佳 SSIM 也相同。新增参数为 4,008（+0.528%），本轮总墙钟增加约 4.0%。

结论：在当前10图、单随机种子、10 epoch 微调筛选条件下，首版“仅依据当前阶段特征，在第3/5/7阶段修正 beta/gamma”的效果与基线基本持平，差值处于千分之一至百分之一 dB 的波动范围，不能视为有效 PSNR 提升，也不应直接投入1000 epoch长训练。下一轮应继续保持单变量原则，优先检查反馈支路是否实际学到非零修正及各阶段修正幅度，再选择“增加浅层结构/梯度提示”或“改变启用阶段”；每次只改一个因素，并继续使用相同基线对照。该结果是方案筛选记录，不进入论文正式主表。

### 10.1 下一步判别流程

在提出第二版结构前，先使用已有 epoch-10 权重完成两项无需训练的检查：（1）在 DIV2K 0801–0900 全部100张验证图上比较最终检查点，排除10图均值的偶然性；（2）统计第3/5/7阶段反馈输出层权重和 delta_beta、delta_gamma 在真实 LR 图像上的平均绝对值、均方根与最大值。新增 repro/inspect_feedback_corrections.py 用于第二项检查。

若反馈修正几乎为零，说明低学习率与零初始化组合可能使新支路没有充分学习，下一轮优先测试“主干1e-5、反馈支路更高学习率”的优化诊断，并与架构创新结果分开记录。若修正已明显非零但完整100图仍无增益，说明仅使用当前阶段特征缺少有效结构指引，下一版再单独加入 LR 梯度/边缘提示。若第二版仍没有至少可重复的约0.02–0.03 dB筛选信号，则停止反馈路线，转向新的核心机制。上述阈值仅用于筛选资源分配，不是统计显著性或论文结论。
### 10.2 反馈修正幅度诊断（2026-09-16）

使用反馈模型 epoch-10 检查点，在 DIV2K LR 0801–0810 上统计真实前向过程中的修正幅度。输出层权重已从零变为非零，说明支路收到梯度并发生学习；但修正量整体很小，且越到后期阶段越弱。

| 阶段 | 输出权重绝对值均值 | 输出权重绝对值最大值 | delta_beta MAE | delta_beta RMS | delta_beta 最大值 | delta_gamma MAE | delta_gamma RMS | delta_gamma 最大值 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 3 | 0.00054829 | 0.00273554 | 0.00020640 | 0.00029015 | 0.00264480 | 0.00008892 | 0.00012799 | 0.00166228 |
| 5 | 0.00050442 | 0.00291917 | 0.00008867 | 0.00011658 | 0.00151965 | 0.00005575 | 0.00007721 | 0.00115553 |
| 7 | 0.00046827 | 0.00230493 | 0.00004342 | 0.00005934 | 0.00100795 | 0.00003017 | 0.00004054 | 0.00064879 |

诊断结论：反馈支路并非完全失活，也没有达到0.1的有界修正上限；其平均修正仅约10^-5至10^-4量级，第3阶段信号最强，第7阶段最弱。这与“全模型统一使用1e-5微调学习率，使零初始化新支路只学到近恒等修正”的解释一致，但尚不能据此证明提高支路学习率一定会提高PSNR。先用现有最终检查点完成100张DIV2K验证图评测；若仍无可辨认增益，再把“主干1e-5、反馈支路10倍学习率”作为优化诊断单独测试，不与结构创新混为一项。
### 10.3 完整100图复核与下一轮优化诊断（2026-09-16）

使用两组epoch-10最终检查点，在DIV2K 0801–0900全部100张验证图上进行单次推理评测。原LFMN为29.248 dB / 0.8447，LFMNFeedback为29.248 dB / 0.8446。PSNR在三位小数下完全相同，反馈模型SSIM低0.0001，确认10图筛选中的+0.004 dB不是可推广增益。首版结构在当前统一1e-5学习率下没有有效提升。

为区分“反馈机制无效”和“零初始化新支路学习不足”，新增feedback_lr_mult优化诊断参数。下一轮保持模型结构、预训练权重、数据、随机种子、主干学习率1e-5和10 epoch预算不变，只让4,008个反馈参数使用10倍学习率1e-4。自动检查确认参数组分别为主干759,627个参数和反馈4,008个参数；一个真实DIV2K batch的加载、反向传播、验证与保存集成测试通过。该项属于训练优化诊断，结果必须与架构创新分开解释。
### 10.4 反馈支路10倍学习率诊断结果（2026-09-16）

保持首版结构、相同公开预训练权重、DIV2K 1–800、验证0801–0810、随机种子1、主干学习率1e-5和10 epoch预算不变，仅将4,008个反馈参数的学习率提高到1e-4。最终及最佳PSNR均为29.788 dB（最佳在epoch 10），最终SSIM为0.8367、最佳SSIM为0.8368（epoch 2），末轮累计L1为6.0983，墙钟32分48秒。

该结果未超过同条件原LFMN的最佳29.791 dB，也未超过统一学习率反馈版本的最佳29.788 dB；与统一学习率版本末轮L1 6.0981相比也几乎相同。因此，“零初始化支路在统一低学习率下没有充分学习”不是当前无增益的主要解释。停止“仅依据prev、在第3/5/7阶段修正beta/gamma”的具体路线，不进行完整100图复核、更多随机种子或1000 epoch训练。下一步应先结合近期轻量超分研究重新定义结构问题，再实现一个单变量候选；本次否定结果保留用于避免重复尝试，不进入论文主表。

## 11. 2024–2026 方向检索与第二轮候选（2026-09-16）

### 11.1 检索范围与边界

本轮以 2024-01 至 2026-09 的公开论文、竞赛报告和预印本为主，重点查看 CVPR/ICCV/NTIRE 的轻量单图超分，并扩展到轻量图像恢复、光场超分和移动端增强。检索用于覆盖主要技术路线，不声称穷尽这两年全球所有论文；2026 年工作中包含尚未经过正式同行评审的预印本，后续引用时必须区分出版状态。

近期可归纳为六类：内容感知稀疏/聚类注意力（CATANet）、频域调制与大核/部分卷积（LKFN）、训练期重参数化与卷积 LoRA（ESPAN、DSCLoRA、NTIRE 2025）、多尺度低到高注意力或上下文融合（LMLT、EchoSR）、卷积模拟自注意力（ConvAttn）、Mamba/状态空间和蒸馏（DVMSR、FSSM）。相邻领域的 MobileIE 表明多分支重参数化和局部方差加权损失可在推理不增支路的前提下提升紧凑模型。

LFMN 当前主干已经直接使用 CATANet 的 TAB/LRSA，实现中 ConvFFN 也已有 5×5 深度卷积。因此，直接再加聚类注意力、普通大核或完整 Mamba 块，创新重叠或延迟风险较高，不列入第一批。扩散、SAM/大模型先验和 LUT 路线也不符合当前“训练与推理都高效、保持同一 LFMN 基线”的首轮约束。

主要依据：

- CATANet，CVPR 2025：https://openaccess.thecvf.com/content/CVPR2025/html/Liu_CATANet_Efficient_Content-Aware_Token_Aggregation_for_Lightweight_Image_Super-Resolution_CVPR_2025_paper.html
- LKFN，CVPRW/NTIRE 2024：https://openaccess.thecvf.com/content/CVPR2024W/NTIRE/html/Chen_Large_Kernel_Frequency-enhanced_Network_for_Efficient_Single_Image_Super-Resolution_CVPRW_2024_paper.html
- NTIRE 2024 Efficient SR 报告：https://openaccess.thecvf.com/content/CVPR2024W/NTIRE/html/Ren_The_Ninth_NTIRE_2024_Efficient_Super-Resolution_Challenge_Report_CVPRW_2024_paper.html
- NTIRE 2025 Efficient SR 报告：https://openaccess.thecvf.com/content/CVPR2025W/NTIRE/html/Ren_The_Tenth_NTIRE_2025_Efficient_Super-Resolution_Challenge_Report_CVPRW_2025_paper.html
- ESPAN，CVPRW 2025：https://openaccess.thecvf.com/content/CVPR2025W/NTIRE/html/Wang_Expanded_SPAN_for_Efficient_Super-Resolution_CVPRW_2025_paper.html
- DSCLoRA，CVPRW 2025：https://openaccess.thecvf.com/content/CVPR2025W/NTIRE/html/Chai_Distillation-Supervised_Convolutional_Low-Rank_Adaptation_for_Efficient_Image_Super-Resolution_CVPRW_2025_paper.html
- ConvAttn，ICCV 2025：https://openaccess.thecvf.com/content/ICCV2025/html/Lee_Emulating_Self-attention_with_Convolution_for_Efficient_Image_Super-Resolution_ICCV_2025_paper.html
- LMLT，ICCVW 2025：https://openaccess.thecvf.com/content/ICCV2025W/AIM/html/Kim_LMLT__Low-to-high_Multi-Level_Vision_Transformer_for_Lightweight_Image_Super-Resolution_ICCVW_2025_paper.html
- MobileIE，ICCV 2025：https://openaccess.thecvf.com/content/ICCV2025/html/Yan_MobileIE_An_Extremely_Lightweight_and_Effective_ConvNet_for_Real-Time_Image_ICCV_2025_paper.html
- EchoSR，2026 预印本：https://arxiv.org/abs/2605.17470
- FreeTransformSR，2026 预印本：https://arxiv.org/abs/2609.05912

### 11.2 候选排序

| 编号 | 候选 | 与 LFMN 的结合点 | 推理代价 | 创新/风险判断 | 当前动作 |
| --- | --- | --- | --- | --- | --- |
| C0 | LRSA 覆盖次数归一化 | 修正重叠 patch 回拼不守恒 | 参数 0，只有覆盖除法 | 是正确性修复，不算创新；预训练权重可能已适应旧行为 | 先做零训练和短微调对照 |
| C1 | 共享高频先验的阶段选择性调制 | 从 `fs` 的局部高通残差只投影一次，各阶段用独立通道门控修正 `beta/gamma` | +3,840 参数（+0.506%）；一次均值池化和一次 1×1 投影 | 与失败的 `prev` 反馈不同；将频域线索、共享先验和阶段选择性结合，但原创性仍需投稿前检索确认 | 第一优先结构短筛 |
| C2 | 高频一致性监督 HFL1 | `L1 + 0.05 × HFL1`，仅训练期约束局部高通残差 | 推理零开销、参数 0 | 低风险但单独创新性弱，主要作为训练策略和 C1 的后续组合项 | 第一优先训练短筛 |
| C3 | 可折叠卷积低秩/多分支增量 | 对 SFML 或中间 3×3 卷积做训练期增量，部署前合并 | 目标为部署零额外分支 | 近期已有 ConvLoRA/GRep/MBRConv，不能直接照搬宣称原创；可作为优化基线 | C1/C2 均不足时再实现 |
| C4 | 低到高多尺度上下文 | 对 TAB/LRSA 的局部—全局交互做分辨率分级 | 预计增加内存重排和注意力 | LMLT/EchoSR 支持可行性，但与现主干功能重叠 | 暂缓 |
| C5 | FSSM/Mamba 替换 TAB | 用线性状态空间建模长程依赖 | 参数可控但内核、显存和真实延迟不确定 | 改动大，不适合第一轮快速归因 | 暂缓 |

C1 暂称“共享频率先验调制”，只是实验名，不在取得稳定结果和完成近邻检索前声称为新方法。其核心不是增加另一条 `prev` 反馈，而是把 `fs - AvgPool(fs)` 形成的显式高频浅层先验进行一次共享投影，再由 8 个阶段的逐通道零初始化门控选择性使用；共享投影避免每阶段重复卷积。零门控保证加载原 LFMN 权重时输出逐元素一致，同时门控从第一步即可收到梯度。

### 11.3 已完成的实现验证

- C1 ×4 参数量 763,467，相对基线 759,627 新增 3,840（0.506%）。加载同一 `scale4_model_939.pt` 后，固定 32×32 输入的初始最大输出差为 0.0；频率门控梯度和为 0.000265；主干 1e-5、频率分支 1e-4 的参数分组为 759,627 / 3,840，检查通过。
- C0 用 57×73、patch 16、step 14 的恒等输入检查：旧回拼最大误差 0.25，覆盖次数归一化后误差 0.0。公开权重、DIV2K 0801、单次推理的零训练快速 A/B 为旧回拼 29.945 dB / 0.8721、归一化 29.947 dB / 0.8721；单图 +0.002 dB 只说明没有明显退化，不能当成提升。
- C2 对相同输入输出为 0，对局部扰动输出正损失 0.003549 且梯度非零。C1 和 C2 均完成真实 DIV2K 单 batch 的加载、反传、0801 验证和保存；单 batch 后都为 29.947 dB，预算过小，不用于判断效果。

### 11.4 高效筛选与停止规则

服务器运行 `bash repro/run_candidate_screen_server.sh 5 810 1e-5`，按顺序执行共同基线、C0、C1、C2。每组同一公开权重、DIV2K 1–800、随机种子 1、HR patch 256、batch 4、5 epoch、0801–0810；C1 仅新分支使用 10 倍学习率，C2 仅把损失改为 `1*L1+0.05*HFL1`。脚本自动汇总最终、最佳、末 3 轮均值和相对基线差值。

10 图短筛只负责淘汰。候选末 3 轮相对基线保持约 +0.01 dB 信号时，才对相同检查点评测 DIV2K 0801–0900；完整 100 图达到约 +0.02 dB 且参数、显存、实测延迟可接受，才扩到 10–20 epoch 和多个随机种子。只有最有希望的一个结构以及匹配基线进入从零上百 epoch；C1 与 C2 组合必须在二者单独有效后才测。阈值是资源分配规则，不等于统计显著性或论文结论。

## 12. RDSM：残差需求监督的逐层调制（2026-09-16）

进一步近邻检索发现，单纯频率分解、波段门控和频率损失在 2024–2026 年轻量 SR 中已较密集，C1 不再作为主创新。新的核心问题改为：LFMN 的八个 SFML 只受最终重建损失间接监督，没有显式目标说明每个阶段“哪些位置仍未恢复”；P01 增加 `prev` 反馈仍失败，也说明仅增加状态输入不能解决监督欠定问题。

新增独立模型 `LFMNRDSM`。训练时，一个八阶段共享的 1×1 残差探针把阶段输入特征映射为 pixel-unshuffle 后的 HR 残差；其剩余误差形成空间需求教师图。共享轻量路由器接收 `Fs`、当前阶段特征和阶段嵌入，预测需求图，并用 `RDEM` 监督。预测图不产生新的 beta/gamma，而是有界地调整原 SFML 相对恒等映射的强度。阶段增益为零初始化，初始输出严格等于公开 LFMN；评测时不运行残差探针。

结构检查：训练态新增 2,761 参数（+0.363%），其中训练期探针 2,352；推理新增仅 409（+0.054%）。残差探针使用零初始化，使第一批需求教师图对应真实未恢复残差而非随机探针误差。固定随机输入的初始最大输出差为 0.0；最终损失到阶段增益、需求蒸馏到路由器、残差监督到探针的梯度均非零。最终实现的真实 DIV2K 单 batch 加权 L1/RRES/RDEM 为 4.65736/0.28812/0.01304，总损失 4.95852；阶段增益已从零更新，0801 验证和模型保存成功。该单 batch 的 29.944 dB / 0.8719 仅证明集成链路正常，不是提升证据。本地 RTX 4060、LR 64×64、预热 3 次并交替计时 10 次时，基线/RDSM 中位延迟为 37.790/39.071 ms，增加 3.39%；正式效率仍需在服务器复测。第一轮不直接比较“基线 vs 完整模型”两组，而是同时测试普通路由、仅深监督和完整 RDSM，以区分深监督收益和需求蒸馏收益。统一总表见 `方案与实验对比总表.md`。
