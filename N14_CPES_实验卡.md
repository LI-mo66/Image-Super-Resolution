# N14 / CPES-SR 实验卡

## 1. 身份信息

```text
候选编号：N14
候选名称：CPES-SR / Cross-Stage Persistent Evidence Sharing Network
候选类型：框架结构 / 信息路由 / 效率
当前状态：APPROVED（仅 D0 诊断；M1 实现 HOLD）
分支：codex/n14-cpes-d0
共同基线提交：b198de7
候选实现提交：未授权进入 M1
负责人/AI：Codex
日期：2026-09-23
```

## 2. 一句话核心假设

由于 LFMN 使用静态浅层先验调制各阶段、但各 TAB 独立建立关系结构，若相邻阶段的关系算子共享低维功能基础且该基础需要动态阶段信息，则可将八阶段重组为四个 Source–Receiver 对，以长期动态证据生成阶段对共享的关系原型，并逐阶段刷新 assignment 与 Value。

## 3. 源码事实与已有反证

- 共同基线为 `LFMN/model/lfmn.py`：48 通道、八个 `SFML→TAB→LRSA→Conv→ESA` 阶段。
- `F_S` 是 32 通道静态浅层先验，八个 SFML 分别生成仿射调制参数；因此 N14 不声称 LFMN 没有浅层持续信息。
- TAB 配置依次为 token 数 16/32/64/128/16/32/64/128，group size 256/128/64/32/256/128/64/32；不同阶段内部 assignment 不可直接交换或按 token 编号比较。
- N13 表明 LR 数据一致性改善不等于正确高频恢复，并且替换原 decoder 会造成显著表示损失；N14 必须完整保留原 decoder、`F0+F8` 和 bilinear 图像残差。
- 未知：相邻 TAB 是否存在统一 LR 空间中的功能冗余；共享关系原型相对独立原型的表达损失；动态阶段信息是否显著优于 `F_S+stage id`。

## 4. D0 诊断假设

```text
R1（必须）：相邻阶段 TAB 在统一 LR 空间的算子响应存在功能冗余。
R2（必须）：共享低秩关系响应基础相对独立基础不会产生明显表达损失。
R3（必须）：等容量条件下，动态阶段信息比静态 FS+stage id 更能预测 TAB 关系响应。
D1（支持）：不同阶段对 HR residual/高频 residual 的冻结主干可恢复性存在差异。
```

D1 不作为实现门槛。D0 使用训练探针，但 LFMN 主干和 B0 权重全程冻结，因此称为“冻结主干诊断”，不称“零训练”。

## 5. D0 方法

### 5.1 统一空间关系算子响应

记录每阶段：

```text
Mi：TAB 输入，B×48×H×W
Ti：TAB 输出，B×48×H×W
Δi=Ti-Mi：统一 LR 空间响应
```

比较相邻阶段的 native-input CKA、公共标准化输入响应 CKA/cosine、空间 top-k 一致率，以及固定噪声/高频扰动下的有限差分响应余弦。所有指标均绕开不可比的内部 token 编号。

### 5.2 共享低秩基础代理

对每张图像、每个候选阶段对，将两阶段响应展平为 `N×48`，比较各自独立 rank-r 空间基础和由拼接响应得到的共享 rank-r 空间基础，扫描 `r∈{8,16,24,32}`。该分解是 R2 的离线代理，不等同于未来 CPES 原型模块。

### 5.3 冻结主干可恢复性探针

八个阶段分别训练结构完全相同的线性探针：

```text
Conv3×3(48→3×4²) → PixelShuffle(4)
```

目标为 `HR-Bicubic(LR)`，并额外报告固定 Laplacian 高频误差。每个 seed 内八个探针从完全相同的初始权重开始；数据、步数和优化器一致，运行 seeds 1/2/3。

### 5.4 静态—动态需求代理

训练两个参数量完全相同的响应预测器：

```text
Static input：标准化 FS(32ch)+stage one-hot(8ch)
Dynamic input：标准化 FS(32ch)+当前 TAB 输入的固定随机投影(8ch)
Target：`Δi` 的逐像素通道 RMS 响应强度，经样本内空间标准化
```

此实验是 R3 的等容量代理。动态输入仍比静态输入包含更多当前信息，结果必须结合 R1/R2 解读，不能单独证明持久状态必要。

## 6. D0 预注册判定

```text
R1 支持：相邻对在公共输入和扰动响应上呈稳定正相似，且逐图 CI 不由少数图像驱动。
R2 支持：某一最小 rank 下，共享基础误差不超过独立基础误差的 1.05 倍；1.05–1.10 为灰区；>1.10 否定。
R3 支持：动态预测 held-out 响应误差相对改善≥10%，逐样本 bootstrap CI 下界>0；5%–10% 为灰区；<5% 否定。
D1：只记录阶段趋势，不控制是否进入 M1。
```

决策：

| R1 | R2 | R3 | D0 决策 |
|---|---|---|---|
| 通过 | 通过 | 通过 | 允许另建 M1 实现分支 |
| 通过 | 通过 | 否定 | 放弃动态状态；不得继续称 CPES |
| 通过 | 否定 | 任意 | N14 主线 NO-GO |
| 否定 | 任意 | 任意 | 相邻 Source–Receiver 配对 NO-GO |
| 任一灰区 |  |  | 只允许补充诊断 |

## 7. 若 D0 通过后的暂定 M1

长期状态 `S_k` 跨四个阶段对更新；由 `S_k` 生成阶段对共享原型 `P_k`；Source 与 Receiver 分别从当前特征重新计算 `A_{k,t}` 和 `V_{k,t}`。完整关系图只作为后续效率消融，不作为默认 M1。原 LFMN LRSA、ESA、PixelShuffle decoder、`F0+F8` 和 bilinear 残差保持不变。

M1 尚未实现，参数、MAC、显存、延迟和性能均未知，不得写成已证实改进。

## 8. 运行资产

```text
诊断实现：repro/n14_d0.py
汇总脚本：repro/summarize_n14_d0.py
服务器入口：repro/run_n14_d0_server.sh
输出目录：experiment/all_runs/<run_name>（Git 忽略）
```
