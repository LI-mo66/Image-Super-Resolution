# LFMN 创新方案与实验对比总表

更新日期：2026-09-16

用途：本文件是全部创新方案、对照实验、否定结果和晋级决定的主索引。每次提出或修改方案后只追加或补全对应条目，不删除失败记录，不把结构验证写成性能提升。详细推导和过程记录仍保存在 `创新思路与实验记录.md`。

## 2026-09-22 N12 / SRPRv2 当前入口

N11 首版从零 20 epoch 对同协议 B0 的末轮差 +0.000822 dB、末 5 轮均差 −0.001668 dB，首版 NO-GO。N12 从共同基线 `b198de7` 建立独立分支 `codex/n12-srprv2`，在 LR 阶段使用持久状态与逐阶段观测残差，并把修正写回下一阶段主干。服务器前审查修复了观测估计不依赖当前 `feat`、Trainer `prepare()` 丢失、诊断 JSONL 换行和 profile 累计 MAC 四处问题。结构、梯度、真实单图训练/评测和保存重载已通过；**N12 尚无正式 PSNR/SSIM 结果**。本机相同 FP32/LR 64×64 profiler 测得 B0/N12 参数 759627/841563、Conv2d MAC 2.833G/3.303G、中位延迟 38.99/49.70 ms；卷积 MAC 不含 bicubic、注意力及插值。先跑与 N9 B0 同协议的 20 epoch，不满足实验卡闸门不延长。详情见 `N12_SRPRv2_实验卡.md` 和 `创新思路与实验记录.md` 第 15 节。

2026-09-22 用户提供 N12 20 epoch 汇总：末轮 PSNR +0.012804 dB，末 5 轮平均 +0.011672 dB，末轮 SSIM +0.000152；逐图胜率 74%，配对图像 bootstrap 95% CI [+0.007166,+0.018843] dB。质量短筛有持续正向信号，较 N11 首版明显改善；原始 manifest、配置、逐图、机制内容和服务器 profile 尚未本机核验，故暂列 **质量通过、总体晋级待审计**。详情见实验记录 15.2；此处不把单 seed 的图像区间当作多 seed 结论。

用户报告已有 N9 B0 40，但当前仓库未登记其服务器路径。N12 的 20→40 续训脚本会搜索并核验 B0 40 的前20轮曲线与本轮 B0 完全配对；若不匹配即停止，不重训 B0。N12 第40轮结果仍待测。

## 2026-09-18 后续研究流程（当前执行依据）

### 核心研究问题

原 LFMN 的共享浅层先验 `fs` 在 8 个 MGRB 中静态不变。当前主线验证：**带原始先验锚定的、由阶段恢复状态驱动的分段先验演化，是否能比静态 `fs` 更好地适配后续重建阶段，同时保持轻量。**

### 候选与顺序

- **A：锚定式分段动态先验**。保留 `fs0`，在阶段边界产生小残差并使用 `fs_i = fs0 + gate_i * update_i(...)`。先测 A1（第4阶段更新一次，5–8复用），再测 A2（第2/4/6阶段分段更新）；只有 A2 有效才测每阶段更新。高频差分只是更新器输入，不单独宣称创新。
- **B：残差式空间门控调制**。仅在 A 通过完整验证后加入。使用以基线为中心的残差门控，避免 sigmoid 只能抑制特征，也不将其表述为未经实现的“交叉注意力”。
- **C：各向异性条形算子**。仅在 A 或 A+B 出现可重复信号后测试。首版保留 TAB+LRSA，在旁路增加零初始化的 `1xk/kx1` 深度卷积分支；必须有等参数、等 FLOPs 普通卷积对照。条形卷积表述为方向性空间建模，不直接等同于高频增强。

### 执行闸门

1. 所有候选同一公开权重、训练数据、随机种子、训练预算、硬件和评测口径；先做结构/恒等/梯度/真实依赖/保存重载检查。
2. 10 图短筛仅用于淘汰；约 +0.01 dB 末3轮信号只允许进入 100 图复核，不是论文结论。
3. DIV2K 0801–0900 若不能稳定约 +0.02–0.03 dB，或 SSIM、显存、真实延迟退化，则停止候选，不进行长训。
4. 只有单模块通过完整验证，才做组合和至少 3 个随机种子；最终再做五个数据集和从零训练对照。
5. 结果产生前不得预填 `+0.3–0.5 dB` 或具体 PSNR 表格；这些是实验目标，不是承诺。

本节替代早期“三重重构直接组合”的执行建议；历史 N1/P01/R01 结果继续保留，不能作为新主线的性能证据。


- 当前主任务：经典单图超分辨率，DIV2K bicubic，×4。
- 公共起点：原 LFMN `scale4_model_939.pt`；×4 参数量 759,627。
- 快速微调筛选：DIV2K 0001–0800，HR patch 256，batch 4，Adam，主干学习率 1e-5，随机种子 1；默认验证 0801–0810，单次推理，无自集成。
- 完整验证：DIV2K 0801–0900，评测为量化后 Y 通道、裁去 scale 像素的 PSNR/SSIM。
- 晋级原则：10 图短筛只负责淘汰；完整 100 图稳定达到约 +0.02–0.03 dB、效率预算合格后，才增加随机种子或从零长训练。该阈值是资源分配规则，不代表统计显著性。
- 正式结论必须固定数据、评测协议、硬件和训练预算，并分开记录结构创新、训练策略和实现修复。

## 2026-09-17 实测追加与纠正（优先于下文早期推断）

- N1 服务器输出 `screen/bidirectional_20260917_120840/`，提交 f157269，5 epoch。同条件 baseline 最终/最佳/末3轮为 29.7849/29.7887/29.7834；candidate 为 29.7842/29.7885/29.7831。原始汇总 final delta=-0.0006、last3 delta=-0.0003（显示值四舍五入相减可能不同），SSIM均0.83672。用户提供的末段墙钟16分02秒不能视为两组总时长。损失逐轮数据尚未收到，无可辨认收益。
- **N1实现偏差**：`(state + projection(final)) - state` 抵消了缓存阶段特征，实质是最终特征的多个可合并1×1残差投影，不是真正双向融合。停止的是这个已运行实现，不能据此否定双向融合概念。初始恒等与非零梯度检查不足以验证设计机制。
- **RDSM-v2截图抄录更正**：direct_full最终29.7823、最佳29.7884、末3轮29.7815、SSIM0.83668；baseline SSIM0.83670，direct_router SSIM0.83671。旧表29.7829/29.7834有误。结论仍为无明确增益，不足以证明监督冲突或核心假设不成立；两轮数据流变化也不支持直接因果归因。
- 复用上述baseline；相同训练预算、权重、数据流、环境和评测协议才可比较。只改变验证范围时重评测已有权重，不重训baseline。脚本不自动重训；历史未保存的hash/环境字段如实注明未知。
- **N3（沿用原编号）中途共享先验更新：待服务器验证**。X4与fs拼接→80→8点卷积→深度3×3→8→32零初始化输出，一次更新fs，仅阶段5–8使用。预计新增1016参数（0.134%）。统一1e-5、L1、5epoch；无蒸馏/附加门控。与P01仍属阶段反馈近邻，区别是共享先验的一次更新及后续重复利用，不能称已证实新颖或有效。
- 服务器检查新增真实依赖测试（非零更新后只改变后半程先验，扰动X4影响输出）、第二步内部梯度与保存重载；另提供同权重关闭更新的诊断。所有深度学习检查由用户在服务器执行，本地仅语法检查。
- 本轮脚本：`repro/run_prior_update_screen_server.sh`；复用基线与逐轮L1/PSNR/SSIM汇总：`repro/summarize_prior_update.py`。无损失记录不推断收敛或趋势。约+0.015 dB仅是扩大验证的资源门槛；不自动长训。
- 评测口径源码复核：当前DIV2K默认benchmark=False，`calc_psnr`实际为RGB、裁scale+6；SSIM为Y、裁scale。之前文档统一描述为Y/裁scale不适用于该DIV2K短筛。此轮保持现有代码不变以复用baseline，benchmark集合单独按其协议报告。

## 总览

| 编号 | 日期 | 方案 | 解决的问题 | 相对基线成本 | 当前证据 | 决定 |
| --- | --- | --- | --- | --- | --- | --- |
| P01 | 2026-09-16 | 当前阶段反馈修正 | 原 SFML 看不到 `prev` | +4,008 参数（+0.528%），墙钟约 +4.0% | 10 epoch 最终 +0.004 dB、最佳 -0.003 dB；100 图 PSNR相同；10×分支学习率仍无增益 | 否定并停止，不长训 |
| C00 | 2026-09-16 | LRSA 覆盖次数归一化 | 重叠 patch 回拼不守恒 | 0 参数；增加覆盖除法 | 恒等误差 0.25→0；单图零训练 29.945→29.947 dB | 正确性对照，不算创新；待短微调 |
| C01 | 2026-09-16 | 共享高频先验阶段门控 | 同一浅层先验缺少显式高频选择 | +3,840 参数（+0.506%） | 初始输出严格等于基线；真实单 batch 链路通过 | 不作为主创新；频率调制近邻工作过密 |
| C02 | 2026-09-16 | HFL1 高频一致性监督 | L1 对局部高频约束不足 | 推理 0 参数、0 额外算子 | 损失与反传、真实单 batch 链路通过 | 仅保留为训练策略候选 |
| R01 | 2026-09-16 | RDSM 残差需求监督逐层调制 | SFML 各阶段缺少”哪里尚未恢复”的直接目标 | 训练 +2,761 参数（+0.363%）；推理 +409 参数（+0.054%）；本地延迟 +3.39% | v1 末3轮 +0.0005 dB；v2（直接路由）末3轮 -0.0018 dB | 两版均否定并停止 |

## P01：当前阶段反馈修正（已否定）

### 假设与实现

在第 3/5/7 阶段从 `prev` 预测 `delta_beta/delta_gamma`，希望当前恢复状态能修正仅依赖共享浅层特征的 SFML。分支为 48→8 的 1×1、深度 3×3 和输出投影，零初始化输出。

### 实验与结果

- 来源权重：`scale4_model_939.pt`。
- 数据集/倍率：DIV2K 0001–0800 微调，0801–0810 短验证及 0801–0900 完整验证，×4。
- 训练：基线和创新组各 10 epoch，L1，主干学习率 1e-5，随机种子 1。
- 10 图：基线最终/最佳 29.784/29.791 dB；反馈最终/最佳 29.788/29.788 dB。
- 100 图：基线 29.248 dB / 0.8447；反馈 29.248 dB / 0.8446。
- 优化诊断：反馈参数使用 10×学习率后最佳仍为 29.788 dB。

### 结论

“无直接监督地从 `prev` 修正 beta/gamma”没有可辨认增益。失败不是单纯学习率不足；该具体机制停止。相关提交：`b091f6d`（实现）、`9344f61`（关闭路线）。

## C00–C02：第二轮低成本候选

### C00 LRSA 回拼归一化

57×73、patch 16、step 14 的恒等输入中，旧实现最大误差为 0.25，覆盖次数归一化后为 0。公开权重在 DIV2K 0801 的零训练单图结果为 29.945→29.947 dB、SSIM 均为 0.8721。单图差值不是性能结论；该项是实现正确性对照，不计作论文创新。

### C01 共享高频先验阶段门控

浅层特征减去 3×3 均值池化得到高频残差，经一次共享 1×1 投影后由阶段通道门控修正 beta/gamma。结构预算和梯度检查通过，但 2024–2026 已出现多种频域调制、双域调制和渐进频率门控方法，因此该简单实现不足以单独支撑论文主创新。

### C02 HFL1

训练损失为 `1*L1+0.05*HFL1`，推理图不变。它可以与有效结构组合，但必须先报告单独消融，不能把训练策略收益归因给结构。C00–C02 引入提交：`8e146c0`。

## R01：残差需求监督逐层调制（RDSM，当前主线）

### 研究问题

LFMN 的八个 SFML 都从同一个 `Fs` 生成调制参数，且只受最终 SR 损失间接监督。各阶段没有明确目标说明当前还有哪些区域未恢复，因此阶段职责可能重复或退化。P01 仅增加 `prev` 输入仍无效，说明“看见状态”不足以形成有效调制，必须给路由器直接而可解释的训练目标。

### 设计

1. 训练期共享残差探针从每个阶段输入特征预测 pixel-unshuffle 后的 HR 残差。
2. 真实 HR 残差与阶段预测残差之差生成空间“剩余重建需求”教师图。
3. 八阶段共享的轻量路由器根据 `Fs`、当前特征和阶段嵌入预测需求图，并由 `RDEM` 直接蒸馏。
4. 预测需求图只调节原 SFML 相对恒等映射的强度，不新建平行特征旁路。
5. 阶段增益零初始化，使公开 LFMN 权重在初始时逐元素保持原输出；推理时完全跳过残差探针。

核心形式：

```text
M_i = beta_i * X_(i-1) + gamma_i
F_i = M_i + a_i(D_i) * (M_i - X_(i-1))
```

其中 `a_i` 初始为 0，之后由阶段增益和预测需求共同决定，可对原调制做有界增强或减弱。

### 结构验证

- 基线参数：759,627。
- RDSM 训练态参数：762,388；新增 2,761（+0.363%）。
- 训练期残差探针：2,352 参数，评测/部署前向不调用。
- 推理新增参数：409（+0.054%）。
- 加载公开权重后的初始最大输出差：0.0。
- 随机小尺寸检查：阶段增益、需求路由器、残差探针三条梯度均非零。
- 残差探针零初始化，使首批教师图对应真实未恢复残差而非随机探针误差。真实 DIV2K 单 batch：加权 L1/RRES/RDEM 分别为 4.65736/0.28812/0.01304，总损失 4.95852；8 个阶段增益均从 0 更新到约正负 1e-4，验证和保存成功。
- 单 batch 后 DIV2K 0801 为 29.944 dB / 0.8719。训练量和图像数都不足，只证明链路与损失量级正常，不视为 PSNR 结果。
- RTX 4060 Laptop、PyTorch 2.9.1+cu128、单张 LR 64×64、预热 3 次并交替计时 10 次：基线/RDSM 中位延迟 37.790/39.071 ms，RDSM 增加 3.39%。这是本地筛选数据；正式延迟需在服务器以同一协议复测。

### 第一轮四组消融

服务器命令：

```bash
bash repro/run_rdsm_screen_server.sh 5 810 1e-5 10
```

| 组别 | 模型 | 损失 | 目的 | 最终 PSNR | 最佳 PSNR | 末 3 轮均值 | SSIM | 结论 |
| --- | --- | --- | --- | ---: | ---: | ---: | ---: | --- |
| R01-B | LFMN | `1*L1` | 同条件微调基线 | 待测 | 待测 | 待测 | 待测 | 待填 |
| R01-A1 | LFMNRDSM | `1*L1` | 无教师的普通路由 | 待测 | 待测 | 待测 | 待测 | 待填 |
| R01-A2 | LFMNRDSM | `1*L1+0.05*RRES` | 仅中间残差深监督 | 待测 | 待测 | 待测 | 待测 | 待填 |
| R01-F | LFMNRDSM | `1*L1+0.05*RRES+0.05*RDEM` | 完整残差需求蒸馏 | 待测 | 待测 | 待测 | 待测 | 待填 |

### 失败归因规则

- A2 有效、完整模型没有额外收益：深监督有效，但需求路由设计无效。
- 路由器不能拟合教师需求图：预测输入、感受野或核大小有问题；先诊断 1×1/3×3/5×5，不直接长训。
- 路由器能拟合教师图但最终 PSNR 不提升：调制作用位置或强度映射有问题。
- 即使使用教师需求的上界实验也无提升：核心“按剩余误差分配调制”的假设不成立，停止路线。
- 只有完整 100 图和多个随机种子均稳定提升，才将 RDSM 写成已验证创新。

## 后续结果追加模板

| 日期 | 编号 | 代码提交 | 权重来源 | 数据集/倍率 | 训练设置 | 参数/FLOPs/延迟 | PSNR/SSIM | 相对基线 | 结论与下一步 |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 | 待填 |


### R01 第一轮短筛实测补充

| 组别 | 最终 PSNR | 最佳 PSNR | 末3轮均值 | 最终 SSIM | 相对基线结论 |
| --- | ---: | ---: | ---: | ---: | --- |
| baseline | 29.7850 | 29.7864 | 29.7846 | 0.83661 | 固定短筛基线 |
| router_only | 29.7877 | 29.7877 | 29.7850 | 0.83661 | 末3轮 +0.0004 dB |
| residual_supervision | 29.7877 | 29.7877 | 29.7850 | 0.83657 | 末3轮 +0.0004 dB，SSIM略降 |
| rdsm_full | 29.7880 | 29.7880 | 29.7851 | 0.83659 | 末3轮 +0.0005 dB，暂不长训 |

AutoDL RTX 4090；共同起点 `scale4_model_939.pt`；每组 5 epoch、每 epoch 1000 batch、batch 4；主干学习率 `1e-5`，RDSM 参数学习率 `1e-4`；验证 DIV2K 0801–0810；总墙钟 16.8 分钟；输出 `experiment/all_runs/screen/rdsm_20260916_150944/`。命令中的 `810` 是验证终点，最后一个 `10` 是 RDSM 学习率倍率。完整模型末3轮仅 +0.0005 dB 且 SSIM未提高，不能证明有效；先诊断路由拟合与实际调制幅度。


### R01-v2：直接残差需求调制（已否定）

- 诊断依据：首版阶段2–8路由/教师相关系数0.70–0.77，但实际修正仅占原调制残差0.007%–0.153%。
- 单变量修改：删除零初始化阶段标量瓶颈；需求末层零初始化为0.5，使用固定0.1上限直接调制，保持初始输出等于基线并允许首步重建梯度进入路由。
- 公平性修复：训练DataLoader改用独立seed生成器，因此重新运行匹配baseline；该修复不计入创新。
- 短筛命令：`bash repro/run_rdsm_direct_screen_server.sh 5 810 1e-5 10`

**实验结果（2026-09-16，服务器 RTX 4090，5 epoch，验证 0801–0810）：**

| 组别 | 最终 PSNR | 最佳 PSNR | 末3轮均值 | 相对基线 delta | 最终 SSIM |
|------|-----------|-----------|-----------|----------------|-----------|
| baseline | 29.7843 | 29.7883 | 29.7833 | +0.0000 | 0.8367 |
| direct_router | 29.7836 | 29.7884 | 29.7825 | -0.0008 | 0.8367 |
| direct_full | 29.7829 | 29.7834 | 29.7815 | -0.0018 | 0.8367 |

墙钟 16.9 分钟，输出目录 `experiment/all_runs/screen/rdsm_direct_20260916_165711/`（服务器）。

**失败归因：**

1. **无监督路由（direct_router）末3轮 -0.0008 dB**，证明移除阶段标量瓶颈本身不能带来增益。
2. **完整监督（direct_full）末3轮 -0.0018 dB**，反而比无监督路由更差，说明 RRES+RDEM 监督可能与主干 L1 存在优化冲突。
3. **与第一版对比**：第一版 rdsm_full 末3轮 +0.0005 dB（虽弱但为正），第二版 -0.0018 dB，移除标量瓶颈后反而退化。
4. **核心假设不成立**："按剩余需求空间分配 SFML 调制强度"在当前实现下无法转化为 PSNR 增益。可能原因：SFML 的端到端优化已经自适应调制，外加需求路由是冗余；或空间需求监督信号与最终 PSNR 之间缺少因果链接。

**结论与决定：**

- RDSM 两版实现均未取得约 0.01 dB 以上的筛选信号，第二版出现退化，停止该路线，不进行长训练。

## 2026-09-19 追加：先验更新关闭与N4阶段差分记忆

- **N3实测关闭**：提交 `1cf0d93`，复用匹配baseline，仅训练候选5 epoch。baseline最终/最佳/末3轮为29.784851/29.788731/29.783422；N3为29.783829/29.787678/29.783096，差值−0.001022/−0.000326 dB，SSIM低0.000010。无可辨认增益，不长训。
- **PriorProxy从零150 epoch关闭**：用户结果包协议为DIV2K×4、0801–0900验证、Cosine `2e-4→1e-6`、seed 1；epoch20/40/60/100/150相对baseline分别−0.009/+0.008/−0.007/−0.003/−0.003 dB，最终SSIM同为0.8357。结果在零点附近反复，不支持先验演化收益。
- **N4实测关闭**：相邻阶段差分经336个通道门控聚合。复用baseline，仅训练候选5 epoch；最终/最佳/末3轮为29.783976/29.789581/29.782797，最终和末3轮差值−0.000875/−0.000626 dB，SSIM低0.000001。只有epoch2短暂领先，没有稳定信号；不做100图或长训。
- **N5百图确认后关闭**：同一baseline epoch-5 checkpoint、0参数、0训练。十图refine1为PSNR +0.006449 dB、SSIM −0.000080；百图严格配对重评测中，固定EMA为29.247425 dB/0.844723，refine1为29.250805/0.844631，即PSNR +0.003380、SSIM −0.000092。正收益随样本扩大而收缩且SSIM稳定下降，不足以支撑主创新；不再测试更多迭代数或benchmark，不训练。
- **N6诊断后关闭**：共享方向交换新增6,480参数（+0.8531%）。候选5 epoch最终/末3轮相对baseline为−0.000301/−0.000164 dB。阶段平均绝对增益已达0.0032–0.0054，ON/OFF输出平均变化0.0795像素，说明模块没有失活；但同一适配主干下打开模块仅+0.000480 dB且SSIM−0.000002，最终仍低于baseline。判定机制与TAB/LRSA/ConvFFN冗余，不做100图或从零训练。
- **N7从零20 epoch关闭**：只替换LRSA回拼后的5×5 DWConv，改为通道对半的1×11/11×1深度卷积与双向交叉门控；TAB不变。参数748,875，相对baseline减少10,752（−1.4154%）。固定DIV2K×4、0801–0900、seed 1及Cosine `T_max=150`，与既有baseline前20轮配对。候选在epoch 12一度+0.038235 dB，但epoch 20/末3轮分别−0.011217/−0.005215 dB，SSIM 0.825870对0.826383；正增益不持续且终点两项指标均退化，不延长到60/150 epoch。
- **N8诊断后关闭**：保留baseline渐进重建头，从`x0`和`fs`固定高通响应增加一次PixelShuffle(4)细节直达路径，并预测HR空间置信度控制注入，新增5,224参数（+0.6877%）。从零20 epoch中15轮PSNR为正，但epoch20仅+0.002962 dB、末3轮−0.000196 dB、SSIM−0.000232。100图配对差为+0.002963 dB，95% CI [−0.002290,+0.008675]且仅51图为正；同权重ON/OFF虽为+0.042942 dB，却来自共同训练主干退化后的补偿。高纹理Q4仅+0.003020 dB、CI跨零、SSIM−0.000373，未支持预设机制。停止40/150 epoch及benchmark调参。
- **N9已实现，待正式训练**：保留TAB原残差与ConvFFN，只将EMA中心、硬分组、IASA/IRCA替换为`Fs+Fm`条件的每图像软Token路由；固定`K=32`、4头、Q/K=24、V=48。A0/A1/C0参数为733,259/759,827/733,867，A0/C0解析路由MAC差1.589%；形状、双输入因果依赖、Token初始分布、梯度、AMP、严格重载、真实单batch训练和逐图指标保存均通过。未运行正式20 epoch，暂无性能结论；四组从零入口与Go/No-Go见`N9_TAB替换结构设计评审.md`。
- 相关提交：`b41d37f`（首版）、`1331e57`（诊断）、`144a3d6`（第二版）。
- 保留完整记录用于避免重复尝试，不进入论文主表。

## 2026-09-23 追加：N12 / SRPRv2 40 epoch 配对结论

- **框架**：八阶段观测残差、LR persistent reconstruction state、proximal feature writeback 与低通道重建修正共同贯穿原 LFMN 主干。
- **协议**：DIV2K 1–800/801–900、×4、HR patch 256、batch 4、seed 1、L1、Adam、Cosine T_max 150；N12与同轨迹B0均从20轮续至40轮。
- **精度**：epoch40 PSNR 28.625822 对 28.630186 dB（−0.004364）；末5轮差值均值+0.007066；epoch40 SSIM +0.000043；逐图均值−0.004357、胜率53%、95% CI [−0.012260,+0.002455] dB。
- **效率**：参数约+10.8%，Conv2d MAC约+16.6%，服务器中位延迟约+45.8%。
- **结论**：终点PSNR、逐图胜率和CI未通过预注册闸门，当前实现关闭，不进入1000 epoch、benchmark或完整消融。保留“跨阶段重建状态”作为未来可重新设计的问题方向，不在N12上继续堆补丁。
