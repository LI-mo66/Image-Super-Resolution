# DIV2K 下载与首轮实验准备（Windows）

更新日期：2026-09-14。本页只处理第一轮 **×4 经典双三次超分**的数据准备。先下载并核对数据；正式训练命令需在数据核验和训练脚本检查后执行，避免使用当前不匹配论文的默认参数。

## 你现在已有的数据

`E:\fuxian_LFMN_jianghe\datasets\benchmark\` 已有 Set5、Set14、B100、Urban100、manga109。它们用于最终评测，**不要放入训练集**，现在不用重新下载。`datasets/` 已被 Git 忽略，不会同步到 GitHub。本机 E 盘在检查时约有 491 GB 可用空间。

## 第一步：只下载这四个 ZIP

到 [DIV2K 官方页面](https://data.vision.ee.ethz.ch/cvl/DIV2K/)下载。该站区分 bicubic（Track 1）和 unknown degradation（Track 2）；本项目当前目标是 bicubic ×4，别选 unknown、x8 或测试集。

1. [训练 HR，800 张：DIV2K_train_HR.zip](https://data.vision.ee.ethz.ch/cvl/DIV2K/DIV2K_train_HR.zip)
2. [训练 LR，bicubic ×4，800 张：DIV2K_train_LR_bicubic_X4.zip](https://data.vision.ee.ethz.ch/cvl/DIV2K/DIV2K_train_LR_bicubic_X4.zip)
3. [验证 HR，100 张：DIV2K_valid_HR.zip](https://data.vision.ee.ethz.ch/cvl/DIV2K/DIV2K_valid_HR.zip)
4. [验证 LR，bicubic ×4，100 张：DIV2K_valid_LR_bicubic_X4.zip](https://data.vision.ee.ethz.ch/cvl/DIV2K/DIV2K_valid_LR_bicubic_X4.zip)

先把 ZIP 保存到 `E:\fuxian_LFMN_jianghe\datasets\_downloads\`。浏览器下载或 PowerShell 下载都可以。若用 PowerShell，在终端逐条执行：

```powershell
$zipDir = 'E:\fuxian_LFMN_jianghe\datasets\_downloads'
New-Item -ItemType Directory -Force -Path $zipDir | Out-Null
$base = 'https://data.vision.ee.ethz.ch/cvl/DIV2K'
curl.exe -L --retry 3 -C - -o "$zipDir\DIV2K_train_HR.zip" "$base/DIV2K_train_HR.zip"
curl.exe -L --retry 3 -C - -o "$zipDir\DIV2K_train_LR_bicubic_X4.zip" "$base/DIV2K_train_LR_bicubic_X4.zip"
curl.exe -L --retry 3 -C - -o "$zipDir\DIV2K_valid_HR.zip" "$base/DIV2K_valid_HR.zip"
curl.exe -L --retry 3 -C - -o "$zipDir\DIV2K_valid_LR_bicubic_X4.zip" "$base/DIV2K_valid_LR_bicubic_X4.zip"
```

若某个下载中断，重复该条命令即可尝试续传。完成后确认四个 ZIP 都有非零大小；不要从不明镜像补齐或混用不同退化方式。

## 第二步：解压到模型要求的位置

把四个 ZIP 分别解压到 `E:\fuxian_LFMN_jianghe\datasets\DIV2K\`。可用 7-Zip“解压到指定目录”，或用 PowerShell：

```powershell
$zipDir = 'E:\fuxian_LFMN_jianghe\datasets\_downloads'
$dst = 'E:\fuxian_LFMN_jianghe\datasets\DIV2K'
New-Item -ItemType Directory -Force -Path $dst | Out-Null
Expand-Archive -LiteralPath "$zipDir\DIV2K_train_HR.zip" -DestinationPath $dst
Expand-Archive -LiteralPath "$zipDir\DIV2K_train_LR_bicubic_X4.zip" -DestinationPath $dst
Expand-Archive -LiteralPath "$zipDir\DIV2K_valid_HR.zip" -DestinationPath $dst
Expand-Archive -LiteralPath "$zipDir\DIV2K_valid_LR_bicubic_X4.zip" -DestinationPath $dst
```

不同解压工具可能多包一层目录。最终必须能找到以下四个示例文件；如路径不同，只移动**解压后的图像文件**到对应目录，不要混放训练和验证图像：

```text
E:\fuxian_LFMN_jianghe\datasets\DIV2K\DIV2K_train_HR\0001.png
E:\fuxian_LFMN_jianghe\datasets\DIV2K\DIV2K_train_LR_bicubic\X4\0001x4.png
E:\fuxian_LFMN_jianghe\datasets\DIV2K\DIV2K_valid_HR\0801.png
E:\fuxian_LFMN_jianghe\datasets\DIV2K\DIV2K_valid_LR_bicubic\X4\0801x4.png
```

## 第三步：核验数量

在 PowerShell 执行：

```powershell
$dst = 'E:\fuxian_LFMN_jianghe\datasets\DIV2K'
(Get-ChildItem "$dst\DIV2K_train_HR\*.png" -ErrorAction SilentlyContinue).Count
(Get-ChildItem "$dst\DIV2K_train_LR_bicubic\X4\*.png" -ErrorAction SilentlyContinue).Count
(Get-ChildItem "$dst\DIV2K_valid_HR\*.png" -ErrorAction SilentlyContinue).Count
(Get-ChildItem "$dst\DIV2K_valid_LR_bicubic\X4\*.png" -ErrorAction SilentlyContinue).Count
```

预期依次为 **800、800、100、100**。再检查上面四个示例路径。若数量不对，先查看解压目录是否重复嵌套或 ZIP 尚未完整下载。

## 第四步：把结果发给我，再进入训练

告诉我这四个计数及示例文件是否存在，我会据此检查数据读取、做短跑并给你一条适合 4060 笔记本的 ×4 训练命令。当前 `LFMN/option.py` 的默认 `--patch_size 192` 是 **HR** 裁块，对 ×4 实际只产生 48×48 LR 裁块；论文使用的是 64×64 LR 裁块，因此正式训练要明确设置 `--patch_size 256`。默认 `--epochs 20`、`--lr 1e-4` 也不同于论文设置，先不要直接运行默认训练。

验证集的官方目录目前还没有被项目的 `DIV2K` 加载器正确接入；这一步需在正式训练前处理。不要把 0801–0900 的验证图像塞进训练目录，也不要用 Set5/Urban100 反复选最佳训练轮次。若下一阶段需要 ×2/×3，再从官方页面补下载对应 bicubic 训练 LR、验证 LR；训练 HR/验证 HR 不用重复下载。

官方页面注明 DIV2K 供学术研究使用，并要求使用时引用其数据集论文；数据图像不上传本仓库。


