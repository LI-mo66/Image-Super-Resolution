# 通用科研创新候选实验卡模板

> 每个新候选复制本文件形成独立记录。开始编码前填写第1–8节，开始服务器训练前完成第9–12节，实验结束后完成第13–15节。

## 1. 身份信息

```text
候选编号（唯一）：
候选名称：
候选类型（结构/训练框架/损失/数据/推理/效率/组合）：
当前状态（PROPOSED/AUDITED/APPROVED/IMPLEMENTED/VERIFIED/SCREENING/DECIDED/PROMOTED/ARCHIVED）：
分支：
共同基线分支/提交：
候选实现提交：
负责人/AI：
日期：
```

## 2. 一句话核心假设

```text
由于________，当前基线系统在________上受到限制；本候选通过________改变这一过程，因此预期________。
```

## 3. 源码事实与问题证据

- 相关源码位置：
- 原始数据流：
- 可测缺陷：
- 已有实验支持：
- 已有实验反证：
- 哪些判断仍是未知：

## 4. 机制与公式

- 输入：
- 状态/表示：
- 更新规则：
- 输出：
- 损失：
- 训练态与推理态差异：
- 为什么这是统一机制而不是模块并列：

## 5. 与共同基线的差异

```text
共同基线：
候选：
保持不变的部分：
被替换的部分：
训练期新增：
推理期新增：
```

## 6. 最近邻与新颖性风险

| 工作 | 相同点 | 实质差异 | 风险 |
|---|---|---|---|
|  |  |  |  |

拟议贡献必须避免的表述：

```text

```

## 7. 复杂度预算

```text
基线参数：
候选参数：
参数变化：
推理MAC/FLOPs变化：
训练额外计算：
预计显存：
预计延迟：
尚未实测字段：
```

## 8. 可证伪预测

若假设成立：

```text

```

若假设失败：

```text

```

机制退化信号：

```text

```

## 9. 基线协议指纹

```yaml
baseline_id:
source_commit:
baseline_model:
pretrained_checkpoint:
checkpoint_sha256:
data_train:
data_range_train:
data_range_validation:
scale:
patch_size_hr:
batch_size:
seed:
augmentation:
optimizer:
learning_rate:
scheduler:
scheduler_horizon_or_milestones:
eta_min_or_gamma:
epochs:
steps_per_epoch:
loss:
rgb_range:
evaluation_datasets:
metric_code_commit:
metric_protocol:
torch_cuda_environment:
result_directory:
```

## 10. 基线复用决定

```text
Baseline reuse: YES/NO
复用的baseline ID：
逐字段比较结果：
已知差异：
为何差异不影响或为何必须重训：
```

## 11. 最小实验矩阵

| 组别 | 模型 | 改动 | 回答的问题 | 何时运行 |
|---|---|---|---|---|
| B0 | 共同基线 | 无 | 统一基线 | 先运行或合法复用 |
| M1 |  | 完整候选 | 主假设是否成立 | 第一阶段 |
| C1 |  | 最简单替代解释 | 是否只是普通机制 | M1通过后 |
| M0 |  | 去掉关键机制 | 关键机制是否必要 | M1通过后 |

## 12. 预注册闸门

```text
Gate 0结构检查：
Gate 1梯度/数值检查：
Gate 2真实单batch：
Gate 3短筛epoch：
短筛通过线：
灰区：
立即停止线：
允许延长到150/200 epoch的条件：
允许1000 epoch的条件：
多seed要求：
五benchmark要求：
```

## 13. 实际服务器协议

```text
服务器/GPU：
branch：
commit：
启动命令：
开始/结束时间：
结果目录：
退出状态：
与预注册差异：
```

## 14. 结果

| 指标 | B0 | M1 | 差值 |
|---|---:|---:|---:|
| 最终PSNR |  |  |  |
| 末5轮PSNR |  |  |  |
| 最终SSIM |  |  |  |
| 逐图均值 |  |  |  |
| 逐图中位数 |  |  |  |
| 正收益比例 |  |  |  |
| bootstrap 95% CI |  |  |  |

机制诊断：

```text

```

## 15. 决策与后续

```text
结论：GO / GRAY / NO-GO
是否超过B0：
是否需要C1/M0：
是否允许延长：
失败归因：
不可宣称的内容：
下一步：
结果记录提交：
```
