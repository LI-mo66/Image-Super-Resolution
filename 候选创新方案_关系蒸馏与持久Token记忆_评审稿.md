# LFMN 后续候选创新方案评审稿

> 版本：2026-09-20
> 用途：提交给其他研究者或 AI 进行独立评审。本文是待验证研究方案，不包含尚未取得的性能结论，也不承诺必然超过 LFMN 或当前先进方法。

## 1. 决策摘要

本项目不应继续围绕共享先验更新、SFML 反馈、小型高频旁路或方向卷积做局部叠加。已有 P01、RDSM、N3、PriorProxy、N6、N7 和 N8 结果对这些假设族形成了较强负证据。后续候选按以下优先级处理：

1. **主方案：可靠性门控的跨阶段关系蒸馏**（Reliability-Gated Cross-Stage Relational Distillation，暂记为 `RGCRD`）。目标是在不增加推理参数和 FLOPs 的前提下，提高 LFMN 的训练上限，并先验证能否稳定超过原 LFMN。
2. **结构备选：共享浅层先验初始化的空间锚定稀疏持久 Token 记忆**（Prior-Initialized Spatially Anchored Sparse Persistent Token Memory，暂记为 `PST-Memory`）。目标是修复 TAB 固定 EMA 原型、不可微硬分组和跨阶段 Token 状态不连续的问题，作为潜在的结构创新主线。

两者不能在第一轮同时加入。先独立证明训练机制或结构机制有效，再讨论组合，否则即使出现提升也无法判断来源。

## 2. 研究目标与公平比较边界

### 2.1 目标

最终希望在相近轻量级约束下，同时做到：

- 超过公开 LFMN 权重的标准五数据集结果；
- 推理参数量控制在约 `0.8M`，FLOPs 接近或不高于 LFMN 的 `57.2G`；
- 在 Urban100、Manga109 等复杂结构/纹理数据集上取得稳定收益；
- 不以明显牺牲 Set5、Set14、B100 为代价；
- 训练数据、评测协议、self-ensemble 设置和模型选择规则全部可追溯。

LFMN 论文报告的 ×4 结果为：Set5 `32.64/0.9004`、Set14 `28.95/0.7893`、B100 `27.80/0.7438`、Urban100 `26.91/0.8087`、Manga109 `31.45/0.9193`，参数约 `760K`，FLOPs `57.2G`。本仓库公开权重在 Urban100 ×4 上复测为 `26.904/0.8087`，说明公开基线基本可复现。

### 2.2 “短筛”和“论文结论”的区别

现阶段不为每个候选训练 1000 epoch。短筛的用途是资源分配，而不是证明一个思想在所有训练条件下永远无效。完整流程应为：

1. 零训练机制诊断；
2. 10–40 epoch 低成本筛选；
3. 150–200 epoch 中程确认；
4. 只有通过前三层闸门的候选，才做同协议 1000 epoch、多随机种子和五 benchmark 正式实验。

附加型训练损失或零初始化残差支路可用预训练微调快速筛选；替换 TAB/LRSA 或改变主干优化路径的结构，必须从零训练至少约 40 epoch 后才能作第一轮资源判断。此前 N7 在 epoch 12 相对基线约 `+0.038 dB`，到 epoch 20 变成约 `-0.011 dB`，已经说明主干替换的极短期排序并不稳定。

## 3. 已有实验给出的约束

### 3.1 已应停止的假设族

- **共享先验/SFML 反馈更新**：P01、RDSM、N3 和 PriorProxy 均未形成稳定增益；其中 PriorProxy 已完成成对从零 150 epoch，最终约 `-0.003 dB`。
- **方向或跨窗口旁路**：N6 的模块确实改变了输出，但相对同权重关闭分支仅约 `+0.00048 dB`，最终模型仍低于基线，说明主要是补偿共同训练后的主干退化。
- **直接高频重建头**：N8 在 DIV2K 100 图上约 `+0.002963 dB`，95% bootstrap 区间跨零，SSIM 下降；高纹理组也没有验证预设机制。
- **测试时 TAB 中心细化**：N5 在 100 图上约 `+0.00338 dB`，同时 SSIM 下降，不足以成为论文主创新。

### 3.2 仍然成立的结构问题

原 TAB 存在三个可以从代码直接确认的问题：

1. 训练时根据当前 batch 特征迭代中心，推理时默认直接使用跨 batch 保存的 EMA 中心，训练和推理的内容适配路径不同；
2. 像素到 Token 的归属使用 `argmax + argsort`，且位于 `no_grad` 中，最终重建损失不能直接优化路由决策；
3. 中心在 batch 维度聚合，不同图像共享一组中心，可能削弱图像特定的结构表达。

这些问题为 Token 路由类结构研究提供了比“缺少高频卷积”更直接的依据。但 N9 当前的独立软路由初始有效 Token 约为 `31.37/32`、Token 质量比约 `2.30`，说明分配较弥散；它还缺少空间锚定和跨阶段 Token 连续性，因此不宜直接把当前实现视为最终设计。

## 4. 主方案：RGCRD

### 4.1 核心假设

LFMN 的推理结构已经具有较强局部、全局和空间调制能力，继续增加推理分支容易与 TAB、LRSA、SFML、ESA 重复。更可能的限制是：只使用最终重建 L1 时，中间阶段没有明确学习“哪些空间关系值得保留、哪些教师知识不可靠”。

强教师包含更成熟的长程关系和纹理组织能力，但教师输出不是绝对真值。若无条件模仿教师像素，学生可能同时继承教师错误。因此主方案仅在教师相对当前学生更可靠的区域蒸馏关系结构，并保留 GT L1 作为主目标。

### 4.2 符号

- 低分辨率输入：`x`；高分辨率真值：`y`。
- 冻结教师输出：`T(x)`；学生输出：`S(x)`。
- LFMN 第 4、8 个 MGRB 的特征：`F_4^S`、`F_8^S`。
- 教师归一化深度约为 `1/2` 和 `1` 处的同分辨率特征：`F_m^T`、`F_e^T`。
- 训练期投影器：`P_i^S(·)`、`P_j^T(·)`，统一投影到 `d` 维；这些投影器不进入推理模型。
- `sg(·)` 表示停止梯度。

### 4.3 局部可靠性门控

先在 HR 输出空间计算局部误差。对位置 `p` 的邻域 `N(p)`：

$$
e_T(p)=\frac{1}{|N(p)|}\sum_{q\in N(p)}\left|T(x)_q-y_q\right|,
$$

$$
e_S(p)=\frac{1}{|N(p)|}\sum_{q\in N(p)}\left|S(x)_q-y_q\right|.
$$

定义软可靠性权重：

$$
m(p)=\operatorname{sg}\left[
\sigma\left(\frac{e_S(p)-e_T(p)-\delta}{\tau_r}\right)
\right].
$$

其中 `δ≥0` 是教师胜过学生所需的安全边界，`τ_r` 控制软门控温度。当教师局部误差明显小于学生时，`m(p)` 接近 1；反之接近 0。停止梯度用于防止学生通过操纵门控而逃避蒸馏。

第一版不应把 `δ`、`τ_r` 变成大量搜索超参数。可先固定 `δ=0`，根据首个 epoch 的误差差值分布设置一个温和的 `τ_r`，随后冻结。

### 4.4 关系表示

为避免完整 `HW×HW` 关系矩阵造成过高训练开销，先将投影特征按固定网格或自适应池化得到 `K` 个描述子：

$$
Z_i^S=\operatorname{Pool}\left(P_i^S(F_i^S)\right)\in\mathbb{R}^{K\times d},
$$

$$
Z_j^T=\operatorname{Pool}\left(P_j^T(F_j^T)\right)\in\mathbb{R}^{K\times d}.
$$

对每个描述子做 L2 归一化，定义关系矩阵：

$$
R(Z)=\hat Z\hat Z^{\mathsf T},
\qquad
\hat Z_k=\frac{Z_k}{\|Z_k\|_2+\epsilon}.
$$

将 HR 可靠性图下采样并池化为 `K` 个权重 `\bar m_k`，关系对 `(a,b)` 的权重为：

$$
W_{ab}=\sqrt{\bar m_a\bar m_b}.
$$

这样只有两个区域都较可信时，它们之间的教师关系才被充分蒸馏。

### 4.5 阶段关系损失

分别对齐学生中段/末段与教师中段/末段：

$$
\mathcal L_{rel}=
\sum_{(i,j)\in\{(4,m),(8,e)\}}
\frac{\left\|W\odot\left[R(Z_i^S)-\operatorname{sg}(R(Z_j^T))\right]\right\|_1}
{\sum_{a,b}W_{ab}+\epsilon}.
$$

该项蒸馏的是空间描述子之间的相对结构，而不是要求异构教师和学生的通道逐元素相等。

### 4.6 跨阶段演化损失

仅对齐两个静态阶段仍不能约束“从中段到末段如何改变”。进一步定义：

$$
\Delta R^S=R(Z_8^S)-R(Z_4^S),
$$

$$
\Delta R^T=R(Z_e^T)-R(Z_m^T),
$$

$$
\mathcal L_{evo}=
\frac{\left\|W\odot\left[\Delta R^S-operatorname{sg}(\Delta R^T)\right]\right\|_1}
{\sum_{a,b}W_{ab}+\epsilon}.
$$

这一项的研究问题是：教师在网络后半程强化或削弱了哪些区域关系，LFMN 是否能学习相同的关系演化，而不复制教师全部特征。

### 4.7 总损失

主方案第一版总损失为：

$$
\mathcal L_{RGCRD}
=\mathcal L_1(S(x),y)
+\lambda_{rel}\mathcal L_{rel}
+\lambda_{evo}\mathcal L_{evo}.
$$

第一轮禁止同时加入高频损失、感知损失、GAN、多教师或新推理模块。`λ_rel` 和 `λ_evo` 应通过首批梯度范数校准，使辅助梯度不压过重建梯度，而不是通过验证集反复搜索最优数值。

### 4.8 推理成本

教师、可靠性图、投影器和关系损失全部仅在训练时使用。部署时仍然执行原 LFMN：

$$
S_{deploy}(x)=\operatorname{LFMN}(x).
$$

因此理论上不增加推理参数、FLOPs和延迟。必须通过导出推理权重、严格加载和参数列表比较验证，而不能只根据设计声称零开销。

### 4.9 教师选择

首选同为 DIV2K-only 的 SwinIR-M ×4 教师，理由是：

- 避免使用 DF2K 或额外数据后把数据优势误写成方法优势；
- 官方权重与代码可获得；
- 教师与 LFMN 架构差异较大，关系蒸馏若有效，更能说明方法不是同构层复制。

教师必须先按本项目标准评测脚本独立验证。若教师在目标数据集或大量训练 patch 上并不优于学生，可靠性门控会自然变稀，此时不应强行提高 KD 权重。

## 5. RGCRD 的对照与判伪

### 5.1 最小短筛组

| 组别 | 模型 | 损失 | 回答的问题 |
| --- | --- | --- | --- |
| B0 | 原 LFMN | L1 | 同条件训练基线 |
| C1 | 原 LFMN | L1 + 普通输出 KD | 强教师本身是否已经足够 |
| M1 | 原 LFMN | L1 + RGCRD | 可靠性关系与跨阶段演化是否优于普通 KD |

只有 `M1 > C1 > B0` 或至少 `M1 > B0` 且 `M1` 明显优于 `C1`，才支持方法创新。若 M1 只超过 B0、却不超过普通输出 KD，则只能说明“教师有用”，不能说明提出的关系机制有效。

### 5.2 通过短筛后才补的消融

- 去掉可靠性门控：检验教师错误过滤是否必要；
- 去掉 `L_evo`：检验跨阶段关系演化是否必要；
- 只蒸馏 stage 8：检验 stage 4 是否提供额外作用；
- 随机教师或打乱关系矩阵：排除辅助正则化本身带来的收益；
- 等训练计算量对照：排除更多反向计算或训练时间造成的差异。

### 5.3 直接判失败的情况

- 可靠性门控长期接近全 0 或全 1；
- 辅助损失下降但重建指标没有相应改善；
- M1 不能稳定超过 C1；
- 收益只来自少数图像，逐图胜率接近随机；
- PSNR 增加但 SSIM、复杂纹理组或五集整体明显恶化；
- 更换随机种子后收益符号反转。

## 6. 结构备选：PST-Memory

### 6.1 动机

当前 N9 在每个阶段独立从 `F_m+F_s` 重新生成 Token，没有跨阶段保持全局语义状态。PromptSR 等工作表明，全局 anchor/prompt 在连续 block 之间传递可能有利于复杂纹理重建。LFMN 又恰好拥有跨全部 MGRB 共享的浅层先验 `F_s`，因此可研究：由 `F_s` 初始化一组有空间锚点的 Token，并使其随八个 MGRB 持续、小步演化。

这一方向不同于 N4 的像素特征差分累加，也不同于当前 N9 的逐阶段独立 Token 重建。

### 6.2 Token 初始化

设空间归一化坐标为 `u_p∈[0,1]^2`，预设 `K` 个规则网格锚点 `c_k`。由浅层先验生成初始 Token：

$$
A_0(p,k)=\operatorname{softmax}_k\left(
q_s(F_s(p))^{\mathsf T}e_k
-\lambda_p\|u_p-c_k\|_2^2
\right),
$$

$$
T_{0,k}=\frac{\sum_p A_0(p,k)v_s(F_s(p))}
{\sum_p A_0(p,k)+\epsilon}.
$$

空间项不是为了把 Token 固定成窗口，而是防止所有 Token 在训练初期变成近似相同的全局平均。

### 6.3 阶段稀疏路由

在第 `i` 个 MGRB，对调制特征 `F_{m,i}` 和前一阶段 Token `T_{i-1}` 计算：

$$
\ell_i(p,k)=
\frac{q_i(F_{m,i}(p),F_s(p))^{\mathsf T}k_i(T_{i-1,k})}{\sqrt d}
-\lambda_p\|u_p-c_k\|_2^2.
$$

只保留每个像素得分最高的 `r` 个 Token，在这 `r` 个位置内重新 softmax：

$$
A_i(p,k)=\operatorname{SparseSoftmaxTopR}(\ell_i(p,k)).
$$

路由熵定义为：

$$
h_i(p)=-\frac{1}{\log r}\sum_k A_i(p,k)\log(A_i(p,k)+\epsilon).
$$

用 `α_i(p)=σ((h_i(p)-η)/τ_h)` 控制 Token 路径强度，使归属明确的简单像素主要保留原 LFMN 路径，高不确定像素获得更多全局 Token 信息。

### 6.4 跨阶段 Token 更新

从当前像素聚合候选 Token：

$$
\widetilde T_{i,k}=
\frac{\sum_p A_i(p,k)v_i(F_{m,i}(p))}
{\sum_p A_i(p,k)+\epsilon}.
$$

使用有界门控做小步更新：

$$
g_{i,k}=\sigma\left(\operatorname{MLP}left[
T_{i-1,k},\widetilde T_{i,k}
\right]\right),
$$

$$
T_{i,k}=T_{i-1,k}
+g_{i,k}\odot\left(\widetilde T_{i,k}-T_{i-1,k}\right).
$$

Token 回写到像素：

$$
H_i(p)=\sum_k A_i(p,k)W_vT_{i,k},
$$

$$
F'_{m,i}(p)=F_{m,i}(p)+\alpha_i(p)W_oH_i(p).
$$

训练和推理必须使用相同的 Token 初始化、稀疏路由和更新过程，不再依赖训练期 batch EMA 中心。

### 6.5 必需的正则与风险

需要监控但不宜一开始叠加过多损失：

- Token 质量分布与有效 Token 数，防止全部像素集中到少数 Token；
- 平均路由熵，防止完全均匀或过早硬化；
- 跨阶段更新量 `||T_i-T_{i-1}||`，防止 Token 不更新或剧烈漂移；
- 空间锚定项占总路由 logit 的比例，防止退化成固定窗口；
- 同权重关闭 Token 路径后的性能变化，判断模块是净贡献还是仅补偿主干退化。

主要风险包括：与 ATD、CATANet、PromptSR 的方法邻近度较高；top-r 路由的工程效率可能不等于理论稀疏；Token 状态可能跨阶段累积错误；复杂纹理收益可能伴随简单区域退化。正式声称新颖性前必须进行更全面的专利和论文检索。

### 6.6 对照组

第一轮只训练原 LFMN 与 PST-Memory 两组。若候选通过筛选，再补：

- 当前 N9 的逐阶段独立软 Token：证明跨阶段持久状态的贡献；
- 无 `F_s` 初始化：证明共享浅层先验的贡献；
- 无空间锚定：证明锚点约束的贡献；
- 等参数/等 FLOPs 普通 mixer：证明收益不是额外容量；
- 非稀疏全量路由：证明稀疏不确定性选择的贡献。

## 7. 统一高效筛选协议

### 7.1 数据和指标

- 训练：DIV2K 0001–0800，×4，LR patch `64×64`，对应 HR patch `256×256`。
- 开发验证：DIV2K 0801–0900，全 100 图，不使用 self-ensemble。
- 同时记录：当前仓库 RGB/crop=`scale+6` 指标，以及标准 Y/crop=`scale` 指标；所有比较只能在相同口径内进行。
- 模型选择不得使用 Set5、Set14、B100、Urban100、Manga109；这些数据集只在候选冻结后一次性评测。
- 保存逐图全精度 PSNR/SSIM，不只保存三位小数平均值。

### 7.2 训练一致性

- baseline 与 candidate 使用相同初始化策略、batch顺序、裁剪、增强、优化器和学习率轨迹；
- DataLoader 使用独立固定生成器；
- 记录代码提交、环境、权重来源和完整配置；
- 主干替换候选从零训练，不用“加载不完整的公开权重后小学习率微调”替代；
- 第一轮只用一个固定 seed 筛选，灰区结果再增加两个 seed，而不是直接用单 seed 写论文结论。

### 7.3 分层闸门

#### Gate 0：零训练/单 batch

- 形状、AMP、梯度、保存重载、严格权重加载通过；
- 机制输入扰动能改变目标中间量；
- 不存在恒等抵消或静默漏载；
- 参数、解析 MAC 和实测延迟均记录。

#### Gate 1：1–2 epoch 机制检查

- RGCRD：可靠性权重不塌缩，辅助梯度与主L1不存在持续强冲突；
- PST-Memory：Token利用率、熵和跨阶段更新量处于非退化状态；
- 此阶段只用于发现实现或优化异常，不根据PSNR小数点后波动决定成功。

#### Gate 2：40 epoch 筛选

建议条件：

- 最后5轮平均 PSNR 相对同条件基线约 `≥ +0.01 dB`；
- 后10轮差值斜率没有持续转负；
- SSIM不出现稳定退化；
- 逐图PSNR胜率建议高于约 `55/100`；
- 逐图bootstrap区间不能显示明确负收益；
- 机制对照与性能方向一致。

这里的阈值只是资源闸门，不是统计显著性或论文结论。

#### Gate 3：150–200 epoch 确认

- 相对基线保持约 `+0.02 dB`或更清晰的稳定增益；
- 至少两个 seed 的收益方向一致；
- 逐图 bootstrap 95% CI 下界为正，或在多个目标数据子集上出现一致收益；
- 参数、FLOPs、显存和实际延迟满足约束；
- 主方法优于最关键的普通对照。

#### Gate 4：论文级训练

- baseline 与最终候选按论文协议各训练1000 epoch；
- 建议3个随机种子；
- 统一评测×2、×3、×4及五个标准benchmark；
- 报告均值、方差、参数、FLOPs、显存、单次推理和self-ensemble结果；
- 分开陈述结构创新收益、训练策略收益和实现修复收益。

## 8. 与近期工作的关系

### 8.1 蒸馏方向

- **MTKD，ECCV 2024**：多教师结合空间与小波域监督，说明 SR 专用蒸馏优于直接套用分类 KD；论文所述最高 `+0.46 dB` 是相对其他 KD 方法，不是对任意强基线的保证。
- **DCKD，AAAI 2025**：根据学生状态动态调整蒸馏空间，支持“蒸馏强度不能固定”的判断。
- **DUKD**：指出教师输出本身是 GT 的有噪近似，支持可靠性筛选而非无条件像素模仿。

RGCRD 的拟议区别是：围绕 LFMN 中间 MGRB 的关系演化，以 GT 相对误差生成停止梯度的局部可靠性门控，并对齐中段到末段的关系变化。该差异仍需正式新颖性检索，不能仅凭组合名称宣称原创。

### 8.2 Token方向

- **ATD，CVPR 2024 / TPAMI**：使用自适应 Token 字典突破局部窗口限制。
- **CATANet，CVPR 2025**：使用内容感知 Token 聚合；LFMN 的 TAB/LRSA 实现与其关系紧密。
- **PromptSR，TMM 2026**：使用低分辨率全局 anchor 生成 prompt，并在连续 block 间传递。其官方结果显示，相近参数下在 Urban100 ×4 和 Manga109 ×4 分别达到约 `27.02` 和 `31.50`，高于 LFMN 的 `26.91` 和 `31.45`，但在几个简单数据集略低，提示全局持续状态更可能改善复杂纹理而不是全面无条件提升。
- **ESC，ICCV 2025**：观察到相邻自注意力层存在较高重复性，以共享大核和动态卷积模拟注意力，说明“跨层共享全局结构”值得诊断；但本项目 N6/N7 对方向卷积已有负结果，因此不建议直接移植 ESC 模块。

PST-Memory 的拟议区别是：使用 LFMN 特有的共享浅层先验初始化空间锚定 Token，并在 MGRB 间持续、小步更新，只对高不确定像素增强 Token 路径。该定义必须通过与 N9、ATD/CATANet式独立字典和 PromptSR式prompt传递的细致比较才能确定新颖性。

## 9. 推荐执行顺序

1. 先实现并筛选 RGCRD 的 B0/C1/M1 三组，因为它不改变部署模型，当前成功概率相对更高，失败成本更低。
2. 只有 M1 稳定优于普通输出 KD，才补可靠性、阶段演化等消融并延长到150–200 epoch。
3. PST-Memory 单独作为结构线开发，第一轮只做 baseline 与完整候选40 epoch对照；不与RGCRD同时加入。
4. 若PST-Memory有稳定信号，再补N9独立Token、无先验、无锚定和普通mixer对照。
5. 最终若RGCRD和PST-Memory分别成立，才测试二者组合，并把“训练策略收益”和“推理结构收益”分开报告。

## 10. 请求独立评审者重点回答的问题

1. RGCRD 的可靠性定义是否存在“利用GT选择教师、却没有新增信息”的逻辑缺陷？关系蒸馏是否足以提供不同于GT L1的优化信息？
2. 教师中段/末段与LFMN第4/8阶段按归一化深度匹配是否合理？是否需要更稳健的自动层匹配？
3. `L_evo` 是否真正约束阶段演化，还是可被投影器轻易吸收？是否应共享投影器或限制其容量？
4. B0/C1/M1是否足以证明主方案，是否必须在短筛阶段加入“无可靠性关系KD”？
5. PST-Memory 与 ATD、CATANet、PromptSR 的实质差异是否足以支撑新颖性？是否已有更近的“persistent token memory for SR”工作？
6. top-r 与熵门控是否会造成优化不连续或部署低效？是否有更简单的稀疏化方式？
7. 40 epoch和150–200 epoch闸门是否能合理预测1000 epoch排序？建议使用何种学习曲线外推或早停统计方法？
8. 在约0.8M参数、57.2G FLOPs、DIV2K-only约束下，当前最合理的论文对手和最低目标应如何定义？
9. 若目标同时包括“较高成功概率”和“足够结构创新”，应该先做RGCRD、PST-Memory，还是其他更有依据的方向？

## 11. 参考来源

- LFMN 本地论文文本：`LFMN_paper_text.txt`
- 本项目总表：`方案与实验对比总表.md`
- 本项目详细记录：`创新思路与实验记录.md`
- MTKD（ECCV 2024）：https://www.ecva.net/papers/eccv_2024/papers_ECCV/html/5614_ECCV_2024_paper.php
- DCKD（AAAI 2025）：https://github.com/super-SSS/DCKD
- DUKD：https://github.com/yun224/DUKD
- SwinIR：https://github.com/JingyunLiang/SwinIR
- ATD：https://github.com/CVL-UESTC/Adaptive-Token-Dictionary
- CATANet：https://github.com/EquationWalker/CATANet
- PromptSR：https://github.com/wenyang001/PromptSR
- HiT-SR（ECCV 2024）：https://github.com/XiangZ-0/HiT-SR
- ESC（ICCV 2025）：https://github.com/dslisleedh/ESC
