# 项目协作约定

- 本仓库根目录是整个 LFMN 复现与创新研究项目的 Git 根目录。勿把 `LFMN/` 或 `repro/catanet_ref/` 重新作为嵌套 Git 仓库加入索引。
- 用户要求：每次 Codex 修改代码或研究文档后，完成适当验证，用明确的提交信息在本地 Git 提交，并在答复中说明修改内容和提交号。用户已要求将项目同步到 `https://github.com/LI-mo66/Image-Super-Resolution`；推送可用时在提交后推送，失败时保留本地提交并报告原因。不要把未经验证的实验结果写成已证实的改进。
- 不提交数据集、实验输出、缓存、环境密钥、参考仓库副本、论文 PDF 或提取的全文。遵守根目录 `.gitignore`，提交前检查 `git status` 与暂存文件列表。
- 实验记录追加到 `创新思路与实验记录.md`，注明来源、模型权重、数据集、倍率、评测口径、训练设置和未知字段；不覆盖原始记录。
- 比较新模型与基线时固定数据和评测协议。算法创新、实现修复、训练策略的结果分开记录。
