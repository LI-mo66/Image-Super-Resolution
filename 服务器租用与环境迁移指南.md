# LFMN 租用 GPU 服务器与环境迁移指南

更新日期：2026-09-16。

## 1. 当前决策

本项目采用“本地笔记本开发与短跑、租用服务器完成长训练”的方式。笔记本 RTX 4060 8GB 的完整 ×4 epoch 实测为 11 分 11 秒；论文设置共 1000 epoch，线性估算约 7.8 天，且后续还需要基线、创新模块、消融和多个随机种子。服务器可以显著减少等待并避免笔记本长期高温，但具体加速倍数必须以同配置 1 epoch 实测，不按显卡理论算力直接推断。

首选平台：AutoDL；首选资源：单卡 RTX 4090 24GB、按量计费。2026-09-16 官方首页显示 4090 约 1.88 元/小时，价格会变化，下单时以页面现价为准。第一次只做约 1 小时测试，不购买周卡或月卡。选择一张 GPU 即可，本项目当前没有多卡训练代码。

## 2. 第一次创建实例时怎么选

1. 打开 https://www.autodl.com/ 注册、实名认证；学生可查看学生认证入口。
2. 进入“控制台 → 容器实例 → 租用新实例”。
3. 计费方式选择“按量计费”。实例从显示“运行中”开始计费，不使用时必须关机。
4. GPU 选择“RTX 4090 24GB”，数量选择 1。
5. 主机优先选择 CPU 至少 8 核、内存至少 32GB、数据盘可用空间至少 50GB 的空闲机器。地区选择网络连接稳定、库存充足的即可。
6. 镜像优先选择 Ubuntu 22.04、Python 3.10、PyTorch 2.5.x 或更高、CUDA 12.1/12.4 的官方基础镜像。若页面没有完全相同的组合，选择 Python 3.10 + PyTorch 2.x + CUDA 11.8 以上，不要选择 TensorFlow、国产 GPU 或多卡镜像。
7. 创建并开机。首次只充值足够完成环境配置和 1 epoch 测试的少量余额。

正式论文中的基线与创新模型必须固定同一种 GPU 型号、镜像、Python/PyTorch 版本、数据、batch、随机种子和评测脚本。服务器与笔记本的速度不能直接比较为模型复杂度结论；本地 epoch-1 PSNR 也不作为服务器正式基线。
本机参考环境为 Python 3.10.20、PyTorch 2.9.1+cu128、CUDA runtime 12.8。服务器不要求逐版本复制这套 Windows 环境；服务器内部的全部正式对照固定在创建时选定的 Linux 镜像，并把实际版本写入实验记录。

## 3. 连接方式

实例开机后，在控制台复制平台给出的 SSH 命令，例如：

```powershell
ssh -p 平台显示的端口 root@平台显示的主机名
```

在本机 PowerShell 执行，第一次询问指纹时输入 `yes`，再粘贴平台密码。密码输入时终端不会显示字符。也可安装 VS Code 的 Remote - SSH 扩展；长训练仍须在 `tmux` 或 `screen` 中运行，避免本地断网终止训练。不要把 SSH 密码、私钥或平台 Token 发到聊天或提交到 Git。

## 4. 下载项目代码

连接服务器后，在服务器终端执行：

```bash
apt-get update && apt-get install -y git curl unzip tmux
cd /root/autodl-tmp
git clone https://github.com/LI-mo66/Image-Super-Resolution.git
cd Image-Super-Resolution
bash repro/setup_server_env.sh
```

`setup_server_env.sh` 不重新安装 PyTorch，而是使用所选镜像自带的 PyTorch，再安装项目所需的固定依赖并打印 GPU、CUDA 和版本。若最后显示 `CUDA available: True` 且 GPU 是 RTX 4090，环境检查通过。

## 5. 下载 DIV2K ×4

在项目根目录执行：

```bash
bash repro/download_div2k_x4.sh
```

脚本会把四个官方 ZIP 下载到 `datasets/_downloads/`，支持中断后重新执行，校验压缩包后解压，并调用 `check_div2k_x4.py` 检查900对图像。

最后必须看到 800/800/100/100 配对通过。数据放在 `/root/autodl-tmp` 数据盘，不放 Git，也不要长期只保留一份服务器副本。

## 6. 先跑 20 batch，再测完整 1 epoch

20-batch 短跑：

```bash
bash repro/train_x4_server.sh scratch 20 1 810 sanity/server_x4_20b
```

通过后运行完整 1 epoch：

```bash
bash repro/train_x4_server.sh scratch 0 1 810 baseline/server_4090_x4_epoch1
```

同时在另一个 SSH 终端监控：

```bash
nvidia-smi --query-gpu=timestamp,temperature.gpu,power.draw,memory.used,utilization.gpu --format=csv -l 5
```

记录服务器 GPU 的完整型号、PyTorch/CUDA 版本、1 epoch 墙钟时间、最高温度、最高显存以及 PSNR/SSIM。先与笔记本相同的 batch 4、10 张验证图比较吞吐；确认无误后再讨论是否调整 batch。不能只因为 24GB 显存更大就直接更改 batch，否则训练设置也改变了。

## 7. 后台训练与关机

长训练建议：

```bash
tmux new -s lfmn
cd /root/autodl-tmp/Image-Super-Resolution
bash repro/train_x4_server.sh scratch 0 1000 810 baseline/server_4090_x4_seed1 2>&1 | tee train_seed1.log
```

按 `Ctrl+B`，松开后按 `D`，可退出 tmux 而不中止训练；重新连接后用 `tmux attach -t lfmn` 返回。首次 1 epoch 测试完成前不要执行 1000 epoch 命令。

训练结束后先把 `experiment/all_runs/...` 中的模型、日志和环境信息下载或备份，再在 AutoDL 控制台关机。官方文档说明实例运行中计费、关机停止计费；连续关机 15 天可能释放实例并清空数据，因此服务器不能作为唯一备份。

## 8. 费用估算方法

第一次测试费用按“实例运行小时数 × 页面单价”计算。以 1.88 元/小时为例，环境配置和 1 epoch 共 2 小时约 3.76 元。完整训练费用必须在服务器实测后计算：

```text
预计费用 = 服务器单 epoch 分钟数 × 1000 ÷ 60 × 当前每小时价格
```

例如实测 3 分钟/epoch，则纯训练约 50 小时、按 1.88 元/小时约 94 元；实测 5 分钟/epoch，则约 83.3 小时、约 157 元。该示例未计入环境配置、验证、排队、失败重跑和数据传输，不能视为报价。

## 9. 官方参考

- AutoDL 快速开始：https://www.autodl.com/docs/quick_start/
- GPU 选型：https://www.autodl.com/docs/gpu/
- 计费说明：https://www.autodl.com/docs/price/
- SSH：https://www.autodl.com/docs/ssh/
- VS Code：https://www.autodl.com/docs/vscode/
- 守护进程：https://www.autodl.com/docs/daemon/
- 数据保留与传输：https://www.autodl.com/docs/instance_data/