# SRPR 阶段特异 Gate 低秩审计卡

## 目的

SRPR 共享边界审计已证明 candidate、gate、writeback 和 q-update 都不能直接跨阶段共享；最佳 donor 仍分别损失 `0.094/0.083/0.172/0.032 dB`。因此本轮保留八个阶段的独立功能，只检查单阶段 gate `W_i ∈ R^(48×99)` 内部是否存在可压缩的低秩结构。

## 对象选择

N12 每阶段 proximal core 参数约为：

```text
candidate: 2576
gate:      4800
writeback: 2416
q_update:   156
```

gate 是最大的单项，且目前是直接 `99→48` 的1×1卷积。candidate/writeback 已有16通道非线性瓶颈，q-update 输出只有3通道；本轮不同时改动它们，以保持单一问题。

## 零训练干预

对已训练 N12 epoch-20 的每阶段 gate 权重执行：

```text
W_i = U_i diag(s_i) V_i^T
W_i^(r) = U_i[:, :r] diag(s_i[:r]) V_i[:r, :]
g_i = sigmoid(W_i^(r) x_i + b_i)
```

原 bias 保持不变。固定 rank 为 `1,2,4,8,12,16,24,32,40,48`；同时用每阶段权重的累积奇异值平方能量选择 `95%,99%,99.5%,99.9%` 自适应 rank。`rank=48` 作为数值重建检查。

## 数据和口径

- checkpoint：N12/SRPRv2 epoch 20，不训练。
- Full 复现：DIV2K 801–900，必须与原 epoch20 逐图 PSNR/SSIM 最大误差 `<=2e-5`。
- 全阶段固定/自适应 rank：DIV2K 801–900，报告 paired PSNR/SSIM、胜率和 bootstrap 95% CI。
- 阶段敏感性：只在 DIV2K 801–820 上将单一阶段截断为 rank 4/8/12，不用于主性能结论。

## 理论预算

原 gate 每阶段参数为 `48×99+48=4800`。真正低秩实现将分解为无 bias 的 `99→r` 和带 bias 的 `r→48`，参数为 `147r+48`。全网候选参数预算：

```text
P(r) = 841563 - 8*4800 + 8*(147r+48)
```

rank 4/8/12/16 时约为 808251/812955/817659/822363，相对 B0 约 `+6.40%/+7.02%/+7.64%/+8.26%`，低于 N12 的 `+10.8%`。这是理论参数预算，延迟必须在真实分解实现后测量。

## 预注册决策

- `PROMISING`：最小的固定或自适应 rank 在100图上相对 Full N12 平均 PSNR `>=-0.003 dB`，bootstrap CI 下界 `>=-0.006 dB`，SSIM `>=-0.00005`，且理论 gate 参数至少减少30%。允许实现从零训练的阶段独立低秩 gate。
- `MARGINAL`：PSNR 为 `[-0.010,-0.003)` 或 CI/SSIM 不满足；只允许结合谱和阶段敏感性决定是否做一个最小实现，不直接长训。
- `REJECT`：所有能显著降参的 rank 均 `<-0.010 dB`；停止低秩压缩路线。

SVD 截断测试的是已训练 gate 的可压缩性，不保证从零分解训练一定恢复或提升性能。本审计不产生新 checkpoint。
